# .ch/docs 目录说明

- `.ch/docs/` 是仓库知识系统，也是默认事实来源。
- 文档优先短、小、可索引；目录页负责导航，主题页负责定规则和沉淀决策。
- 重要文档应说明：用途、更新触发条件、相关入口。
- 代码、接口、运行方式、架构、质量判断发生变化时，要同步回写文档。
- 自动生成内容统一放在 `.ch/docs/generated/`，不要和手写规则混在一起。
- 如果信息还只存在聊天、会议、口头结论里，那它对代理来说仍然“不存在”。
