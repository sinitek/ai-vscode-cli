# 功能总表

这个文件是当前仓库功能清单的**单一事实来源**。

作为 starter，这里默认从空表开始；当首个真实能力进入规划或实现时，再新增条目。

## 更新规则

- 新增能力时新增一行。
- 修改已有能力时更新状态、角色、规格来源、测试状态和备注。
- 下线能力时不要直接删除，改成 `removed` 并保留历史说明。
- 如果一个功能变更无法直接链接到规格，至少要能链接到执行计划或设计文档。
- 不要让 README、聊天记录、任务单、临时表格分别维护不同版本的功能列表。

## 什么时候必须更新

- 新增用户可感知能力、后台能力、运维能力或平台能力。
- 修改已有能力的行为、权限、流程、入口、状态或适用角色。
- 能力进入暂停、废弃、替换、下线状态。
- 需求没有新增页面，但改变了导入导出、通知、审计、报表或批处理能力。

## 状态枚举

- `proposed`：已识别，但尚未进入实施
- `in-progress`：正在建设
- `active`：已经交付且当前生效
- `deprecated`：仍存在，但不建议继续使用
- `removed`：已下线，仅保留历史记录

## 维护边界

- 功能名描述用户或系统能力，不直接使用页面名或接口名。
- 测试状态只回答是否有基础自动化护栏，不替代测试报告。
- 执行计划应说明本次是否需要更新本表；收尾说明应记录实际变更项。

## 当前清单

starter 默认不预置功能项。复制模板后，请从第一个真实能力开始维护下表。

| 业务域 | 功能名称 | 状态 | 主要角色 | 规格来源 | 实现位置 | 测试状态 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AI 对话 / Loop | 子任务手动恢复完成收尾一致性 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/lobsterSubtaskLifecycle.ts`、`src/extension.ts` | `npm run build`；`node --test dist/test/lobsterSubtaskLifecycle.test.js dist/test/sessionMessageActions.test.js` | 子任务手动中断后在子任务 Tab 继续，若成功结束则与自动重试成功复用同一状态落盘和自动关标签流程；关闭设置、再次错误或停止时不自动关标签，主任务仅在可恢复且未触发连续失败上限时唤醒 |
| AI 对话 / Loop | 开发级子任务高级 Skill 指导 | active | Loop 主智能体、子智能体 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `media/loop-workflow-skills/manifest.json`、`src/lobsterSkillGuidance.ts`、`src/lobsterTaskStore.ts`、`src/lobsterPromptBuilders.ts`、`src/extension.ts`、`src/lobsterDebateRunner.ts` | `node scripts/sync_loop_workflow_skills.js --check`；`node scripts/validate_loop_workflow_skills.js`；`npm run build`；round-5 指定 9 组 Node tests 共 143/143 通过 | 仅明确分类为 development 的 Loop 根任务向主智能体提供有界 compact catalog；普通主从和红蓝首轮/后续轮同源覆盖，主智能体只选 ID，宿主按 allowlist、阶段、任务类型、角色、能力、资源完整性和预算校验后持久化 Markdown 快照，子任务 model prompt 注入且 display 不展示正文，自动重试复用。non-development、unknown、legacy、资源失败或无合法 ID 均静默回退原 Loop 直接安排。资源固定从扩展安装根的 `media/loop-workflow-skills/` 读取；首版无 UI、i18n、用户配置，也不扫描 Home、workspace 或 cwd 替代目录。发布前仍需执行 `vsce ls` 与实际 VSIX 解包逐项核对 |
| AI 对话 / Loop | 子任务疑问静默转交 | active | Loop 主智能体、子智能体 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/extension.ts`、`src/lobsterTaskStore.ts` | `npm run build`；`node --test dist/test/lobsterSkillIntegration.test.js dist/test/lobsterTaskStore.test.js` | 子任务遇到需求不明、授权不足、依赖/写入冲突或必须确认的问题时立即停止实施，把问题、事实、影响、选项与建议写入沟通文件的“待主任务确认”章节，按 completed 正常结束并只输出固定中性收口文本；不得在用户可见 assistant 回复中提问或复述问题。主任务读取后自行决策、继续派发，确需用户或人工确认时走 blocked 路径 |
| AI 对话 / 记忆 | Harness 骨架开关与踩坑记录 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`docs/LONG_TERM_MEMORY_DESIGN.md` | 工具设置、prompt 构建、当前工作区 `.ch/.agents` harness scaffold、`AGENTS.md`、`CLAUDE.md`、`.gitignore` 的 `.codegraph/` 忽略项、`ARCHITECTURE.md` AI 初始化任务、`.ch/docs/memory/`、`.ch/docs/runbooks/PITFALLS.md`、CodeGraph 终端初始化 | `npm run build`；`node --test dist/test/toolSettings.test.js dist/test/memoryRuntimeGate.test.js dist/test/longTermMemory.test.js` | 默认关闭；开启时先确认，确认后补齐工作区 harness scaffold、确保 `.codegraph/` 被 git 忽略，并启动 CodeGraph 设置；收尾阶段可二次确认并复用当前 AI 对话以 coding 模式初始化 `ARCHITECTURE.md`；`PITFALLS.md` 记录带根因/规避/验证线索的失败、阻塞、回滚或明确踩坑总结；关闭后只允许查看/导出/删除，不控制 Codex / Claude / OpenCode 外部 CLI 自带记忆或历史 |
| AI 对话 / Codex | Codex 官方 multi_agent 与子代理流式气泡 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | 工具设置、`src/interactive/codexRunner.ts`、`src/interactive/codexRunnerRuntime.ts`、`src/interactive/codexAppServerEvents.ts`、`src/subagentProgress.ts`、Webview 消息路由 | `npm run build`；`node --test dist/test/codexAppServerEvents.test.js dist/test/codexRunnerRuntime.test.js dist/test/subagentProgress.test.js dist/test/finalConclusion.test.js` | 默认关闭；关闭时扩展显式禁用 Codex 官方 `multi_agent`。开启后按 App Server `threadId` 把 `item/agentMessage/delta`、`collabAgentToolCall`、`subAgentActivity` 和子线程完成状态写入各自独立 assistant 气泡；并发子代理交错输出不会合并，子线程不会覆盖/结束父线程，子代理文本不参与父任务 final-answer 判定 |
| AI 对话 / 通用 | 最终答复显式协议与判定策略 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | 全局工具设置 `finalAnswerPolicy`、`src/finalAnswerProtocol.ts`、共享 prompt 构建、Codex app-server 回合事件适配、OpenCode JSONL `stop` 终态关联、最终结论气泡判定 | `npm run build`；`node --test dist/test/finalAnswerPolicy.test.js dist/test/promptRuntime.test.js dist/test/finalConclusion.test.js dist/test/codexRunnerRuntime.test.js dist/test/opencodeCommandRunner.test.js dist/test/toolSettings.test.js dist/test/sessionMessageActions.test.js` | 默认 `strict_final_answer`：结构化 final 类型优先；Codex 接受 `phase="final_answer"`，OpenCode 接受同一 `messageID` 的非 thinking assistant `text` + `step_finish.reason="stop"`。没有结构化类型时，Codex / Claude / OpenCode 当前用户消息之后包含 `[final_answer]` 的非 thinking assistant 文本也视为最终答复。普通任务 prompt 和 hidden retry prompt 都要求模型仅在完成任务后的最终回复以该标记开头；Loop 等内部机器协议继续使用自身结构化终态，不注入或要求文本标记。可切换 `successful_reply_fallback`，额外接受成功退出后的普通助手答复；旧 `codexFinalAnswerPolicy=completed_turn_fallback` 自动迁移到兼容策略 |
| AI 对话 / 通用 | 最终答复协议标记展示过滤 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | Webview assistant 气泡渲染 | `npm run build`；`node --test dist/test/finalAnswerPolicy.test.js` | 仅从 assistant 气泡的展示文本移除 `[final_answer]`；不修改用于严格判定、会话续接和存档的原始消息，user 气泡保持原样 |
| AI 对话 / Codex | 推理摘要空注释精准清洗 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/codexReasoningContent.ts`、Codex app-server reasoning 解析、Codex thinking 会话清洗 | `npm run build`；`node --test dist/test/codexReasoningContent.test.js dist/test/codexAppServerProtocol.test.js dist/test/codexRunnerRuntime.test.js` | 仅从 Codex reasoning/thinking 内容移除独占一行的空 HTML 注释 `<!-- -->`（含水平空白变体）；普通 assistant/user 内容、行内空注释和非空 HTML 注释保持原样；加载历史 Codex 会话时同步清理已落盘残片 |
| AI 对话 / 通用 | 内部思考 wrapper 定向清洗 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/thinkingMarkup.ts`、OpenCode JSONL text/reasoning 解析、Codex reasoning 与历史消息清洗 | `npm run build`；`node --test dist/test/opencodeCommandRunner.test.js dist/test/codexReasoningContent.test.js` | 仅识别 `<thinking>`、`<think>`、`<analysis>`、`<reasoning>` 四类内部 wrapper；混合 text 事件拆成 thinking 与 assistant，最终正文排除思考块，历史消息去除 wrapper；普通 `<div>` 等 HTML/代码标签保持原样 |
| AI 对话 / OpenCode | `todowrite` 任务列表与工具标题本地化 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/cli/openCodeTaskList.ts`、`src/cli/commandRunner.ts`、`src/openCodeTabStream.ts`、`src/extension.ts`、`src/webview/viewContentScript/taskListAndUi.ts`、`src/webview/viewContentScript/coreRuntimeState.ts`、`src/webview/viewContentScript/traceRendering.ts`、`src/webview/viewContentI18n.ts` | `npm run build`；`node --test dist/test/openCodeTaskList.test.js dist/test/openCodeTabStream.test.js dist/test/openCodeTaskListOverlay.test.js dist/test/opencodeCommandRunner.test.js dist/test/traceToolTitleLocalization.test.js` | OpenCode 从 JSONL `tool_use/todowrite` 的 `state.input.todos`（兼容 metadata/output）实时更新当前或并行 conversation tab 的任务列表，空数组清空；专用 `taskListUpdate` 与同一 tool trace 的任务元数据形成双通道，Webview 重载时会重放运行中列表，运行中的消息刷新不再清空浮层；常见 tool-use 气泡标题按中英文界面本地化，未知工具与详情正文保持原样 |
| AI 对话 / OpenCode | 并行与 Loop 子任务实时对话气泡 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/openCodeTabStream.ts`、`src/cli/commandRunner.ts`、`src/extension.ts`、conversation tab 消息存储与 Webview 定向消息协议 | `npm run build`；`node --test dist/test/openCodeTabStream.test.js dist/test/opencodeCommandRunner.test.js dist/test/conversationTabLock.test.js dist/test/conversationTabPagination.test.js` | 并行/Loop 子任务不再只显示原始流：stdout JSONL `text`、`reasoning` / `step_start`、`tool_use` 分别实时生成对应 tab 的 assistant、thinking、trace 气泡并保留任务元数据；`todowrite` 继续定向更新任务列表；退出时完整 final text 仅补齐未展示尾部，避免重复整段回答 |
| AI 对话 / OpenCode | 子代理事件同步与 60 秒补捞气泡 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/cli/openCodeSubagentMonitor.ts`、`src/subagentProgress.ts`、`src/extension.ts`、`src/i18n.ts`、one-shot / 并行 tab / Webview 消息路由 | `npm run build`；`node --test dist/test/openCodeSubagentMonitor.test.js dist/test/subagentProgress.test.js dist/test/finalConclusion.test.js dist/test/openCodeTabStream.test.js dist/test/opencodeCommandRunner.test.js` | OpenCode 父 `run --format json` 不转发内部子代理增量时，插件连接 run 自带本地服务：SSE 事件触发子会话消息快照刷新，并每 60 秒通过公开 children/message/status API 全量补捞；每个当前尝试新建的子 session 固定更新独立 assistant 气泡，展示执行中/完成/失败/中断，多个子代理不串流。若 60 秒仍无子会话最多提示一次；不读取私有 SQLite，子代理气泡不参与父任务 final-answer 与 hidden retry 成功判定 |
| AI 对话 / OpenCode | 当前工作区 cwd/PWD 一致执行 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/cli/commandRunner.ts`、`src/extension.ts` | `npm run build`；`node --test dist/test/opencodeCommandRunner.test.js` | OpenCode one-shot、并行和 Loop 子任务以当前 VS Code 工作区为 spawn cwd，并把 child env `PWD` 强制同步为同一路径；即使 extension host 继承 `PWD=/`，OpenCode `run` 内部请求也不会再把新会话创建到文件系统根目录或要求用户重新选择项目；修复前已绑定 `/` 的历史会话需新建一次对话，不直接篡改 OpenCode 数据库 |
| AI 对话 / 会话 | 当前会话提示词倒序查看 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | 对话运行状态区“提示词”按钮、当前会话消息运行态、提示词弹层 | `npm run build`；`node --test dist/test/runPromptHistoryWebview.test.js` | 仅展示当前 tab 会话的非空用户输入；按 `createdAt` 倒序，缺少时间时按消息顺序倒序；最新提示词置顶 |
| AI 对话 / 会话 | 会话 Tab 分页边界按钮 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/webview/viewContentScript/messageRendering.ts`、`src/webview/viewContentStyles/headerTabs.ts` | `npm run build`；`node --test dist/test/conversationTabPagination.test.js` | 超过 5 个 tab 时每页最多显示 5 个；第一页只显示“下一页”，中间页显示两个方向，最后一页只显示“上一页”；不可用方向直接隐藏，不渲染为 disabled 按钮 |
| AI 对话 / 会话 | 历史会话 Loop 标记与统一恢复入口 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | 历史记录“历史会话”列表、Loop 任务与会话绑定、输入区“打开群聊”按钮 | `npm run build`；`node --test dist/test/historySessionLoop.test.js dist/test/loopmaingroupchatbutton.test.js` | 通过 Loop 任务持久化的 `cli + sessionId` 精确标记 Loop 会话；移除重复的“Loop 群聊”恢复 tab，加载会话后使用现有“打开群聊”按钮进入群聊 |
| AI 对话 / 配置中心 | 三组 CLI 可视化配置、参数提示与范例入口 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/exec-plans/active/2026-07-12-cli-config-visualization.md`、`.ch/docs/references/cli-runtime-reference.md` | Claude / OpenCode / Codex 配置卡片可视化参数、配置文件名区域、配置页诊断说明 | `npm run build`；`node --test dist/test/codexConfigVisualEditor.test.js dist/test/claudeConfigVisualEditor.test.js dist/test/opencodeconfigvisualeditor.test.js`；raw value/排序定向 `dist/test`；`node --check media/config/assets/config-app-ui.js` | 三组可视化参数 label 右侧提供问号 tooltip，枚举提示列出当前候选，三组“查看范例”统一位于配置文件名右侧。Codex 新增 `model_verbosity`、`web_search`、`wire_api` 等受控单选和 `developer_instructions` 多行文本；Claude 将 `permissions.defaultMode` 升级为完整单选，并新增稳定布尔/行为字段；OpenCode 新增顶层 `share`/`autoupdate`/`logLevel`/`snapshot` 受控字段，`model`/`small_model`/`npm` 改为可编辑组合框，reasoning effort 改为动态可输入 tags。三组均保留原生 TOML/JSON 源码、未知值和复杂配置保护；`AugmentExtensionSidecar` 403 仍按外部扩展侧请求失败口径处理 |
| AI 对话 / OpenCode | OpenCode CLI、会话续接与配置驱动双模型 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | AI 对话 CLI 分组、OpenCode JSONL `sessionID` 提取、tab 会话接管、active config 模型解析、主模型/小模型下拉、按配置隔离的角色覆盖、运行时 config overlay、运行时 profile 日志、会话存档 | `npm run build`；`node --test dist/test/opencodeCommandRunner.test.js dist/test/opencodedualmodelwebview.test.js`；OpenCode parser/Webview/runner/runtime/store/config example tests | 首轮接管真实 `ses_*`，后续同 tab 通过 `--session` 续接；`local_*` 只用于插件消息暂存且不会传给 CLI，旧占位会话捕获新真实 ID后迁移消息和 tab 引用；候选只从 active config 的 `provider.<id>.models` 加载且无管理入口；聊天区 OpenCode 模型区按两行展示：第一行为“大模型” label + 主模型 + 主模型思考力度，第二行为“小模型” label + 小模型 + 小模型思考力度；正常 option 只显示 `models.<id>.name`（缺失时回退 model id）；选择配置默认 ref 会清除临时覆盖，其他项发送 exact ref；普通、并行、Loop 主/子任务、续跑和唤醒统一使用 effective primary，小模型和主/小模型思考力度通过随机 `OPENCODE_CONFIG` overlay 注入；`opencode-runtime-profile` 及启动日志记录 primary/small model 与两套 variant；one-shot 只在启动后 60 秒完全无 activity 时超时，首个有效事件后不再以父 JSONL 静默判断卡死，避免分组/子代理任务误杀；覆盖按 config id 隔离并清理失效值，overlay 权限为目录 `0700`/文件 `0600`，exit/error/timeout/cancel 后清理且不改写用户配置 |
| AI 对话 / OpenCode | coding / Loop 双模式入口 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/design-docs/vscode-cli-extension-runtime.md` | 对话面板模式选择器、`src/sessionMessageActions.ts`、Loop 主任务/子任务/多轮复核/群聊编排、active config effective primary 运行链路 | `npm run build`；OpenCode Loop 模式入口与路由回归测试 | OpenCode 可在对话面板选择 coding 或 Loop；Loop 复用既有主任务、子任务、多轮和群聊链路，并通过非交互式 one-shot `opencode run` 执行。该能力不表示 `isInteractiveSupported(opencode)=true`，不启用 Codex/Claude interactive runner 或 common command；OpenCode 不使用 Codex 专用主/子任务模型分配，所有对话和 Loop 请求使用下拉选择的 effective primary，小模型仅供 OpenCode 内部轻量请求；Loop 多智能体执行模式下拉统一位于输入区底部操作图标左侧，与 Codex/Claude 布局一致 |
| AI 对话 / Loop | CLI 模型选择与编排角色解耦 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md`、`.ch/docs/design-docs/vscode-cli-extension-runtime.md` | Webview 模型区域、`sendPrompt.model`、Loop 普通轮次/辩论/续跑/唤醒运行链路、模型状态兼容读取 | `npm run build`；`node --test dist/test/opencodedualmodelwebview.test.js dist/test/opencodeloopmodewebview.test.js dist/test/opencoderolemodelruntime.test.js dist/test/sessionMessageActions.test.js` | Claude 在 Coding/Loop 均不显示插件侧模型选择；Codex 在 Coding/Loop 复用同一个模型下拉，主任务、子任务、裁判、参与者、续跑与唤醒全部使用同一 `model`；OpenCode 在 Coding/Loop 保持自身大模型/小模型及独立思考力度，`small_model` 不等于 Loop 子任务模型；旧主/子模型存储字段仅兼容读取，不再进入 PanelState、消息 payload 或运行时 |
| AI 对话 / Loop | Loop 主任务 Tab 生命周期运行态与队列约束 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/design-docs/vscode-cli-extension-runtime.md` | `src/sessionTabs.ts`、`src/extension.ts`、`src/webview/viewContentScript/messageRendering.ts`、`src/webview/viewContentScript/runStreamAndQueue.ts` | `npm run build`；`node --test dist/test/conversationTabLock.test.js dist/test/loopPromptQueue.test.js dist/test/sessionMessageActions.test.js` | Loop 主任务 tab 使用 `☀️` 图标、子任务 tab 使用 `🌛` 图标，均不额外拼接 `Loop` 文本；任务记录仍为 `running` 时，主 Tab 持续显示运行态并保持不可关闭，不依赖当前是否存在 AI/CLI 执行进程；该 tab 的新提示词直接入队，阶段性进程结束不提前出队；只有任务变为 `completed` 才自动继续队列，人工复核、错误或停止时保留队列并解除 tab 锁定 |
| AI 对话 / Loop | Loop 群聊用户任务与发言气泡 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/lobsterDebate.ts`、`src/panelDiagnostics.ts`、`src/webview/lobsterDebatePanel.ts`、`src/webview/lobsterDebatePanelRenderer.ts`、`src/webview/lobsterDebatePanelStyles.ts` | `npm run build`；`node --test dist/test/lobsterDebate.test.js dist/test/lobsterDebatePanel.test.js` | 创建或恢复任务时，任务记录中的原始 `rootPrompt` 会作为最顶部、标记为“任务发起”的右侧“我”气泡展示，不重复写入群聊 transcript；未完成且可补充需求的任务显示“我要说话”，提交内容写入任务记录和主通信文件，刷新后按提交顺序继续显示右侧“我”的对话气泡，并隐藏内部时间和轮次元数据 |
| AI 对话 / OpenCode | 精确主模型动态推理 variant | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/cli/openCodeModelCapabilities.ts`、`src/modelSelectionStore.ts`、`src/panelStateBuilder.ts`、`src/extension.ts`、`src/cli/commandRunner.ts`、Webview thinking selector | `npm run build`；相关 OpenCode resolver/config/Webview/runner/action/integration tests | 主模型与小模型思考力度分别来自对应精确模型 CLI metadata 或当前配置显式声明；主模型可用 `--variant`，小模型因 CLI 无 `--small-variant`，由 runtime config overlay 写入对应模型 `options.reasoningEffort`；未知模型 Default-only，不按 npm/provider/model 名猜测，`--thinking` 仅控制 thinking blocks |
| AI 对话 / Gemini | Gemini CLI 支持下线 | removed | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | 旧 AI 对话 Gemini 分组、旧配置中心 Gemini 卡片、旧 `geminiStreamJson` / catalog 路径 | 文档校验：`rg -n "Gemini\|gemini" <授权文档>`；源码验证由对应实现子任务负责 | Gemini 已从当前 AI 对话和配置中心支持范围移除；旧路径和脚本只作为历史迁移或审计参考，不再作为当前支持 CLI |
| AI 对话 / 上下文 | 执行后自动压缩上下文 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | 工具设置、`src/contextCompactionRunner.ts`、`src/extension.ts`、Codex/Claude/OpenCode interactive runner 或 OpenCode 非交互 slash command fallback | `npm run build`；`node --test dist/test/contextCompactionRunner.test.js`；OpenCode runtime/UI 子任务复核运行链路 | 默认开启；仅成功结束且执行超过 5 分钟的已有会话触发；自动压缩为静默后台任务，不追加普通任务完成耗时气泡、不覆盖刚完成任务的真实执行时间；手动压缩仍显示压缩运行状态；OpenCode 官方 TUI slash commands 支持 `/compact`（alias `/summarize`）用于 compact current session，配置 `compaction.auto` 默认开启，插件侧可复用当前实现链路或在非交互受限时 fallback 发送官方 slash command |
| 配置 / 配置中心 | CLI 配置档案管理与卡片级保存 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/exec-plans/active/2026-07-12-cli-config-visualization.md` | 配置中心 webview、`media/config/assets/config-app-ui.js`、`src/config/configService.ts`、`src/webview/configPanel.ts` | `npm run build`；`node --check media/config/assets/config-app-ui.js`；`node --test dist/test/codexConfigVisualEditor.test.js dist/test/claudeConfigVisualEditor.test.js dist/test/opencodeconfigvisualeditor.test.js` | 支持配置档案列表、排序、激活、删除、初始化、备份、导出、Skills 和 MCP 管理。Codex 卡片管理 `~/.codex/config.toml` 与独立 `.env`，保持 TOML 可视化/源码模式，受控单选、布尔和多行文本仅定向写入已管理字段，复杂 TOML 与 legacy/未知值保留。Claude 卡片管理用户级 `~/.claude/settings.json`，保持 JSON 可视化/源码模式；稳定 boolean、行为单选、完整 `permissions.defaultMode`、模型/网关自由文本和可编辑列表并存，额外 `env`、hooks、复杂权限和未知字段不丢失。OpenCode 模型/Provider 卡片只维护 `~/.opencode/config.json`，全局 MCP 市场单独维护 `${XDG_CONFIG_HOME:-~/.config}/opencode/opencode.json` 顶层 `mcp`，不提供 `.env` 第二保存入口；其顶层稳定字段使用继承语义控件，动态 model/npm 使用可编辑组合框，reasoning effort 使用可输入多值 tags，未知顶层/provider/model/options/复杂 variants 与无效 JSON 保护保持。思考力度显示 raw value；Codex 固定新建值为 `minimal / low / medium / high / xhigh / max / ultra`，`max` 紧跟 `xhigh`；Claude 固定新建值以 `ultra` 结束，存量 `max` 仅兼容显示在 `ultra` 前；OpenCode 精确 provider/model 动态 variants 保持 payload 原值和顺序 |
| 配置 / MCP | OpenCode 官方全局配置文件 MCP 管理 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/config/openCodeMcpConfig.ts`、`src/config/mcpService.ts`、MCP 市场、`media/mcp_marketplace.json`、`scripts/validate_mcp_marketplace.js` | `npm run validate:mcp-marketplace`；`npm run build`；`node --test dist/test/openCodeMcpConfig.test.js dist/test/configMcpService.test.js dist/test/opencodeMcpHealth.test.js dist/test/mcpInstalledStatus.test.js dist/test/mcpMarketplaceCatalog.test.js` | 安装和卸载直接管理 `${XDG_CONFIG_HOME:-~/.config}/opencode/opencode.json` 顶层 `mcp`；local 使用命令数组与 `environment`，remote 使用 URL 与 `headers`；安装保留其他顶层字段和已有 MCP，卸载只删除目标键且幂等；JSON/JSONC 解析失败不覆盖原文件，成功修改原子写回；MCP 市场打开时仅从 Claude / Codex / OpenCode 各自配置文件快速读取安装状态，健康检测由按钮单独触发，`opencode mcp list --pure` 仅用于 OpenCode 健康检测；内置市场已压缩刷新为 GitHub、Microsoft Learn、Playwright、Docker Gateway、Cloudflare、Stripe、Sentry、MongoDB、Grafana、Elastic、Slack、Notion、Linear、Brave、Atlassian 等官方/权威候选，校验脚本阻止旧 `modelcontextprotocol/servers/tree/main/src/` 路径和旧 `@modelcontextprotocol/server-*` 包名回流 |
| 配置 / 配置中心 | 窄屏配置目录初始展开 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | 对话面板配置按钮、`media/config/assets/config-app-ui.js`、配置中心 `920px` 响应式布局 | `npm run build`；`node --check media/config/assets/config-app-ui.js`；`node --test dist/test/configappcompactcontrols.test.js` | 从配置按钮首次打开配置中心且当前视口小于等于 `920px` 时，左侧配置目录默认展开；用户仍可通过关闭按钮、遮罩或 `Esc` 收起，宽屏初始状态不变 |
