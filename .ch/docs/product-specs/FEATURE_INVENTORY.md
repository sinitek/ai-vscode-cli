# 功能总表

这个文件是当前仓库功能清单的**单一事实来源**。

作为 starter，这里默认从空表开始；当首个真实能力进入规划或实现时，再新增条目。

## 更新规则

- 新增能力时新增一行。
- 修改已有能力时更新状态、角色、规格来源、测试状态和备注。
- 下线能力时不要直接删除，改成 `removed` 并保留历史说明。
- 如果一个功能变更无法直接链接到规格，至少要能链接到执行计划或设计文档。
- 不要让 README、聊天记录、任务单、临时表格分别维护不同版本的功能列表。

## 什么时候必须更新

- 新增用户可感知能力、后台能力、运维能力或平台能力。
- 修改已有能力的行为、权限、流程、入口、状态或适用角色。
- 能力进入暂停、废弃、替换、下线状态。
- 需求没有新增页面，但改变了导入导出、通知、审计、报表或批处理能力。

## 状态枚举

- `proposed`：已识别，但尚未进入实施
- `in-progress`：正在建设
- `active`：已经交付且当前生效
- `deprecated`：仍存在，但不建议继续使用
- `removed`：已下线，仅保留历史记录

## 维护边界

- 功能名描述用户或系统能力，不直接使用页面名或接口名。
- 测试状态只回答是否有基础自动化护栏，不替代测试报告。
- 执行计划应说明本次是否需要更新本表；收尾说明应记录实际变更项。

## 当前清单

starter 默认不预置功能项。复制模板后，请从第一个真实能力开始维护下表。

| 业务域 | 功能名称 | 状态 | 主要角色 | 规格来源 | 实现位置 | 测试状态 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AI 对话 / 通用 | P0 性能与内存硬化 | active | 终端用户、扩展运行时 | `docs/PERFORMANCE_MEMORY_AUDIT_2026-07-13.md`、`.ch/docs/design-docs/vscode-cli-extension-runtime.md`、`.ch/docs/exec-plans/completed/2026-07-31-p0-performance-memory-hardening.md` | `src/extension.ts`、`src/interactive/claudeRunner.ts`、`src/cli/commandRunner.ts`、`src/openCodeTabStream.ts`、`src/webview/viewContentScript/*`、`src/webview/panelFileActions.ts` | `npm run build`；`node --test dist/test/claudeRunner.test.js dist/test/extensionDeactivateStopAll.test.js dist/test/boundedText.test.js dist/test/commandRunnerCoverage.test.js dist/test/opencodeCommandRunner.test.js dist/test/openCodeTabStream.test.js dist/test/clipagescriptruntimecoverage.test.js dist/test/sessionMessageHandlersCoreCoverage.test.js` 114/114；`git diff --check` | Claude stop 将 AbortController 传给 SDK；deactivate/reload 统一 stop-all；OpenCode raw stdout/stderr 与 JSONL pending line 有界并使用增量 activity tracker；Run Stream 有记录数/字节预算且 overlay 关闭时不构建完整 DOM；Assistant delta 流式阶段轻量更新；附件上传最多 10 个文件、单文件 20 MiB、不限制总大小，Webview 和 Extension Host 双端校验并提供中英文超限提示 |
| AI 对话 / 通用 | 计费终态错误立即收口 | active | 终端用户 | `.ch/docs/references/cli-runtime-reference.md`、`.ch/docs/runbooks/PITFALLS.md` | `src/panelDiagnostics.ts`、`src/interactive/codexRunner.ts`、`src/interactive/codexRunnerRuntime.ts`、`src/extension.ts` | `npm run build`；`node --test dist/test/panelDiagnostics.test.js dist/test/codexRunnerRuntime.test.js dist/test/hiddenRetry.test.js` | HTTP 402、`Payment Required`、明确积分/余额耗尽不进入 hidden retry，首次失败即保留原始 provider 错误并收口；Codex 可见错误使用结构化 `error` trace，不能作为正常进度清零重试计数。429、网络中断和普通工具 trace 继续沿用既有重试与恢复语义 |
| AI 对话 / OpenCode / Loop | 主任务空响应有界恢复 | active | 终端用户、Loop 主智能体 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/openCodeRunCompletion.ts`、`src/extension.ts`、`src/loopTaskStore.ts` | `npm run build`；`node --test dist/test/openCodeRunCompletion.test.js dist/test/opencodeCommandRunner.test.js dist/test/hiddenRetry.test.js dist/test/sessionMessageActions.test.js dist/test/loopTaskStore.test.js dist/test/loopPromptBuilders.test.js dist/test/promptRuntime.test.js` | OpenCode 进程 `code=0` 但当前尝试没有非 thinking assistant 正文时，one-shot 与 parallel 路径进入既有 hidden retry；Loop 后续轮次只认当前进程尝试的正文，不使用历史主任务 JSON 误判成功。对于无 provider error、已有远端 session 的 Loop 主任务，首次空成功响应会在下一次重试新建 session 并重发完整主提示，随后保留旧历史、改绑 tab 和任务存储到新 session；其它路径不切换。重试耗尽后显示并持久化明确错误，合法当前回复继续进入 `LoopMainDecision` 校验 |
| AI 对话 / Loop / 会话 | Loop 任务 Tab 状态收敛与会话重置一致性 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/design-docs/vscode-cli-extension-runtime.md` | `src/extension.ts`、`src/loopDebate.ts`、`src/sessionTabs.ts`、Webview 事件绑定 | `npm run build`；`node --test dist/test/conversationTabLock.test.js dist/test/loopDebate.test.js dist/test/sessionMessageActions.test.js` | 当前扩展实例仍拥有 Loop 编排或相关 CLI 运行时，主任务 Tab 继续禁止关闭和重置；持久化为 `running` 但已无运行所有权的残留记录会收敛为 `stopped` 并解除锁定。重置会话只在扩展端完成“新建空白 Tab + 关闭旧 Tab”后刷新前端，拒绝操作不会伪装成已清空 |
| AI 对话 / Loop / 续跑 | 主任务 Tab 中断后输入续同一任务 | active | 终端用户 | `.ch/docs/references/cli-runtime-reference.md`、`.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/sessionMessageActions.ts`、`src/extension.ts`、`src/panelDiagnostics.ts` | `npm run build`；`node --test dist/test/sessionMessageActionsCoreCoverage.test.js dist/test/panelDiagnostics.test.js` | Loop 主任务 Tab 关联的任务处于 `needs-review`、`error`、`stopped` 或可恢复 `running` 状态时，在该 Tab 下继续发送任意 Loop 输入都会作为本次继续说明复用原 `taskId`，语义等同群聊“继续执行”；只有找不到可恢复任务时才允许新建 Loop 任务。Loop 子任务 Tab 手动输入仍强制走 coding 续跑，不启动新的 Loop 根任务 |
| AI 对话 / Loop | Loop 最大轮次全局设置与续跑提升 | active | 终端用户 | `.ch/docs/references/cli-runtime-reference.md`、`.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/extension.ts`、`src/loopTaskStore.ts` | `npm run build`；`node --test dist/test/loopTaskStoreCoreCoverage.test.js` | `loopMaxRounds` 保存在全局工具设置，默认 20、范围 1-100。新建任务写入当前值；已有任务在恢复、继续或下一轮运行前若记录上限低于当前全局设置，会提升任务记录的 `maxRounds`，因此可把旧 20 轮任务扩展到新的上限；降低全局设置不会回写降低已有任务，避免运行中任务被意外截断 |
| AI 对话 / Loop | 子任务手动恢复完成收尾一致性 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/loopSubtaskLifecycle.ts`、`src/extension.ts` | `npm run build`；`node --test dist/test/loopSubtaskLifecycle.test.js dist/test/sessionMessageActions.test.js` | 子任务手动中断后在子任务 Tab 继续，若成功结束则与自动重试成功复用同一状态落盘和自动关标签流程；再次错误或停止时不自动关标签，主任务仅在可恢复且未触发连续失败上限时唤醒 |
| AI 对话 / Loop | 开发级子任务高级 Skill 指导 | removed | Loop 主智能体、子智能体 | `.ch/docs/exec-plans/completed/2026-07-12-loop-subtask-skill-injection.md` | 已删除 `media/loop-workflow-skills/`、`src/loopSkillGuidance.ts` 及同步/校验脚本 | 已由本次移除回归覆盖 | 2026-07-15 下线；不再加载或注入内置 Workflow Skills，也不提供开关 |
| AI 对话 / Loop | 子任务项目规则隔离与最少轮次执行 | active | Loop 主智能体、子智能体 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/loopSubtaskExecutionRoot.ts`、`src/extension.ts`、`src/cli/commandRunner.ts`、`src/interactive/claudeRunner.ts` | `npm run build`；`node --test dist/test/loopSubtaskExecutionRoot.test.js dist/test/opencodeCommandRunner.test.js dist/test/loopPromptBuilders.test.js dist/test/loopPromptQueue.test.js dist/test/loopMainFailure.test.js dist/test/loopSubtaskThinking.test.js` | 主任务保留真实工作区和项目规则；子任务在隐藏根规则与项目 Skills 目录的临时根中执行，并使用各 CLI 的隔离参数。子任务遵循主任务下达的自包含范围，在单一连续执行回合内完成实现，只运行直接证明本次改动的最小必要检查 |
| AI 对话 / Loop | 子任务疑问静默转交 | active | Loop 主智能体、子智能体 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/extension.ts`、`src/loopTaskStore.ts` | `npm run build`；`node --test dist/test/loopPromptBuilders.test.js dist/test/loopPromptQueue.test.js` | 子任务遇到需求不明、授权不足、依赖/写入冲突或必须确认的问题时立即停止实施，把问题、事实、影响、选项与建议写入沟通文件的“待主任务确认”章节，按 completed 正常结束并只输出固定中性收口文本；不得在用户可见 assistant 回复中提问或复述问题。主任务读取后自行决策、继续派发，确需用户或人工确认时走 blocked 路径 |
| AI 对话 / Graph | Graph 可视运行图、恢复与最小控制 | active | 终端用户 | `.ch/docs/design-docs/graph-orchestration-mode.md`、`.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/exec-plans/completed/2026-07-23-graph-phase2-recovery-visualization.md`、`.ch/docs/exec-plans/completed/2026-07-24-graph-ai-planned-dag.md` | `src/graph/`、`src/extension.ts`、`src/promptRunState.ts`、`src/sessionMessageActions.ts`、`src/sessionMessageHandlers.ts`、`src/panelDiagnostics.ts`、`src/panelStateBuilder.ts`、`src/webview/graphRunPanel*.ts`、主 Webview 模式选择和消息 action | `npm run build`；`node --test dist/test/graphRunPanel.test.js` 19/19；`git diff --check` | Webview 可选择 Graph，后端先创建 planning-only Graph run，由 `plan` AI planner 在 `## JSON` 输出 `plannedGraph.nodes` / `plannedGraph.edges` 后 materialize 为真实 DAG；支持分支、fan-out/fan-in、测试、评审、sleep、merge、summary、项目工作区 direct 执行；新 planner 拒绝 `human_gate` / `human_approved` / `manual`、Graph tab `🗺️`、主 tab 图级运行态、独立节点 conversation tab、可视 DAG 面板、主 Graph tab 唯一普通打开入口、指定/最近 run 打开、Continue/Resume、“我要说话”、Retry、direct 自动返工、Stop、sleeping auto wake；节点返回 blocked 会归一为 failed 并进入 retry / `if_fail` / failed 复核路径，不再弹出 blocked modal 或跳过下游 quick pick；历史 worktree run 仍保留 metadata 读取、checkpoint/merge-back/cleanup 兼容处理，新 Graph run 不再创建 worktree/checkpoint/merge-back。主 Graph tab 的视觉运行态跟随图级生命周期，`sleeping` 等等待态仍显示运行中；failed / 历史 blocked 节点导致的 `needs-review` 会让主 tab 进入错误态并释放运行态；主 Graph tab 右下角 AI 对话“中止”和 GraphRunPanel Stop 共用同一 Graph stop 控制链，会把 run / active node 状态落盘为 `stopped`，并防止异步 tick 用旧状态覆盖 stopped。可视 DAG 使用对齐目标工作流画布的 `@dagrejs/dagre` full-canvas left-to-right layered layout，支持按当前节点尺寸调优的层间距/节点间距/边距、布局后碰撞消解、长标题/多下游节点动态高度、dagre 失败时拓扑层级兜底、12-port 自动连线、每个方向只渲染一条代表边、正反向边使用不同连接点、仅渲染流程边、隐藏 `evidence_for` / `conflicts_with` 等非流程连线、依赖边不显示可见文字、非依赖流程边保持正向单行短标签（完整说明保留在 title/aria/list）、已经过边按类型高亮：`depends_on` 保持 VS Code 主题蓝色，`if_pass` 绿色、`if_fail` 红色、`review_feedback` 黄色、`human_approved` 橙色，未经过边保持原样、Start/Decision/End/Step 语义 chip、按 `node.kind` 使用 VS Code 主题变量着色的矩形类型卡片、用户拖拽节点微调当前 run 的可视位置、背景左键按住拖拽平移，节点拖拽按 zoom 比例换算，边 path/端口属性/标签坐标/画布同步重算，拖拽移动不误触发详情，节点位置和 zoom 按 `graphRunId` 本地持久化；右上角缩放下拉固定 25%、50%、75%、100%、125%，默认 75%，Reset 仅为紧凑控件，不再作为占空间长文案且不重置 zoom。新增单击节点详情弹窗，详情弹窗承载节点详情、控制入口和 Evidence/证据区；新运行时不再生成 human gate 审批 CTA。新增证据区展示选中节点 artifact、communication file、验收 evidenceRef、事件和 finalAnswer evidence 引用。review 节点 prompt 会从上游节点 `writeFiles`、communication file 和 artifactRef 生成评审范围，要求按候选改动文件过滤 git status/diff，范围外 dirty workspace 改动不单独导致 failed。Graph completed 后主 Graph tab 会追加 `graphFinalSummary=true` 的 assistant 最终总结气泡，内容来自主模型执行的 `summary` 节点 `finalAnswer`。Graph UI 不再常驻展示 Stop 能力边界说明；具体 Stop 操作结果或错误仍通过运行消息/状态反馈表达。限制：无图编辑器/模板库/运行前确认/DAG 结构编辑/边节点编辑/完整人工审批工作流/复杂布尔条件编辑器/自动条件重规划/rollback 预演/证据文件正文读取；背景平移、12-port、非依赖流程边短标签、语义 chip 和类型着色矩形卡片仅为视觉层优化；direct 模式无 checkpoint/merge-back/git rollback，Retry 只在当前工作区状态上重跑节点；direct run 若失败分类推荐 `direct_rework` 且存在 active `review_feedback` / `if_fail` 显式返工边，会自动重置声明返工范围并继续调度；Feedback rollback 只对历史 worktree/baseCommit 可用的旧 run 提供；Stop 只保证 Graph 状态和事件落盘，真实 CLI 停止仅对已映射 active run 发起请求且不确认退出 |
| AI 对话 / Graph | AI planner 并行优先 DAG 规划 | active | 终端用户、Graph planner | `.ch/docs/design-docs/graph-orchestration-mode.md`、`.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/exec-plans/completed/2026-08-02-graph-planner-parallelism.md` | `src/graph/graphPlanner.ts`、`src/graph/graphPromptBuilders.ts`、`src/graph/graphScheduler.ts` | `npm run build`；`node --test dist/test/graphPlanner.test.js dist/test/graphPromptBuilders.test.js dist/test/graphScheduler.test.js` 22/22；`git diff --check` | AI planner prompt 默认要求先寻找可并行分支，把多个可拆分且互不冲突的任务设计成 planner fan-out、test/review/merge/summary fan-in 的 DAG；不得仅因列表顺序或共享最终验收串行化独立分支。planner 漏填 `plannedGraph.maxConcurrent` 时，materialize 会按首批仅依赖 `plan` 且通过 scheduler 冲突判定的可执行根节点推断并发上限；同一 `conflictGroup`、重叠 `writeFiles` 或未声明写入范围的写节点仍保守串行，上限不超过既有 5 个。 |
| AI 对话 / Graph | Advisory 验证节点不中断收束 | active | Graph planner、终端用户 | `.ch/docs/design-docs/graph-orchestration-mode.md`、`.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/exec-plans/active/2026-08-02-graph-advisory-validation.md` | `src/graph/types.ts`、`src/graph/graphPlanner.ts`、`src/graph/graphStore.ts`、`src/graph/graphScheduler.ts`、`src/graph/graphPromptBuilders.ts`、`src/graph/graphFailureClassification.ts` | `npm run build`；`node --test dist/test/graphScheduler.test.js dist/test/graphPlanner.test.js dist/test/graphStore.test.js dist/test/graphFailureClassification.test.js dist/test/graphPromptBuilders.test.js` | 完整单测、全仓测试、全量 lint 等覆盖面大且可能包含历史/范围外失败的验证节点可声明 `blocking:false`，失败后仍保留 failed 状态并进入 review/summary 的 evidence 与 unresolved，但不会单独阻断结构依赖继续执行；失败分类会把 `dist/test/*.test.js` 候选映射回 `src/test/*.test.ts`，便于后续返工判断真实源码测试范围。 |
| AI 对话 / Graph | 条件边与验证失败反馈返工 | active | 终端用户 | `.ch/docs/design-docs/graph-orchestration-mode.md`、`.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/exec-plans/completed/2026-07-24-graph-validation-feedback-rollback.md` | `src/graph/types.ts`、`src/graph/graphPlanner.ts`、`src/graph/graphStore.ts`、`src/graph/graphScheduler.ts`、`src/graph/graphRunControl.ts`、`src/extensionHost/graphRuntime.ts`、`src/graph/graphPromptBuilders.ts`、`src/webview/graphRunPanel*.ts` | `npm run build`；`npm run validate:whitespace -- src/graph/types.ts src/graph/graphFailureClassification.ts src/graph/graphRunControl.ts src/extensionHost/graphRuntime.ts src/test/graphRunControl.test.ts src/test/graphFailureClassification.test.ts src/test/graphExtensionRuntime.test.ts src/test/trailingWhitespaceCheck.test.ts scripts/check_trailing_whitespace.js src/extensionHost/promptOneShotRuntime.ts`；`node --test dist/test/graphRunControl.test.js dist/test/graphFailureClassification.test.js dist/test/graphExtensionRuntime.test.js dist/test/trailingWhitespaceCheck.test.js` 29/29；`node --test dist/test/graphScheduler.test.js dist/test/graphNodeLifecycle.test.js dist/test/graphStore.test.js dist/test/graphNodeArtifact.test.js` 34/34 | Edge 记录和 planner edge spec 保留 `label`、`conditionExpression`、`metadata`；scheduler 支持 `source_status`、`source_acceptance` 条件求值；`manual` 仅历史兼容，新 planner 会拒绝生成；custom/不可求值条件保守进入失败/复核口径且输出可读 blocker。历史 worktree/baseCommit 可用的 failed / 历史 blocked 验证类节点可 Feedback rollback；宿主优先按 active `review_feedback` / `if_fail` edge 或上游依赖选择返工目标，记录 target selection、候选、reset scope、feedback reason 和触发 edge，被重置节点写入 `rework`，后续 prompt/summary 可见返工来源和范围；direct run 不提供 Feedback rollback 按钮或 checkpoint rollback，但若失败分类推荐 `direct_rework` 且显式反馈边可用，会自动重置声明 scope、返工目标和失败源节点并继续调度。仍不提供边编辑器、局部返工路径编辑、自动修复分支、自动条件重规划或可视 rollback 预演 |
| AI 对话 / Graph | Graph 失败分类与 needs-review 返工建议 | active | 终端用户、Graph planner | `.ch/docs/design-docs/graph-orchestration-mode.md`、`.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/runbooks/PITFALLS.md` | `src/graph/types.ts`、`src/graph/graphFailureClassification.ts`、`src/graph/graphNodeLifecycle.ts`、`src/graph/graphStore.ts`、`src/graph/graphNodeArtifact.ts`、`src/extensionHost/graphRuntime.ts`、`src/extension.ts`、`scripts/check_trailing_whitespace.js` | `npm run build`；`npm run validate:whitespace -- src/graph/types.ts src/graph/graphFailureClassification.ts src/graph/graphRunControl.ts src/extensionHost/graphRuntime.ts src/test/graphRunControl.test.ts src/test/graphFailureClassification.test.ts src/test/graphExtensionRuntime.test.ts src/test/trailingWhitespaceCheck.test.ts scripts/check_trailing_whitespace.js src/extensionHost/promptOneShotRuntime.ts`；`node --test dist/test/graphRunControl.test.js dist/test/graphFailureClassification.test.js dist/test/graphExtensionRuntime.test.js dist/test/trailingWhitespaceCheck.test.js` 29/29 | 失败节点会落盘 `GraphNodeRecord.failure`，`node.failed` event data 写入 `failureClassification`；needs-review / idle 文案展示 category、confidence、signals、recommendedRecovery、recommendedWriteFiles 和 nodeDraft。重构/迁移导致旧测试契约失效且验证节点缺少测试 `writeFiles` 时，分类为 `missing_write_scope` 并保留 `stale_test_contract` signal，推荐新增测试契约适配节点而不是单纯 Retry；direct run 中带显式 `review_feedback` / `if_fail` 返工边的实现缺陷推荐 `direct_rework`，避免继续输出不可执行的 direct checkpoint rollback 建议；`validate:whitespace` 覆盖 untracked 新文件行尾空白，避免评审节点重复失败 |
| AI 对话 / Graph | GraphRunPanel 目标系统式自动布局升级 | active | 终端用户 | `.ch/docs/design-docs/graph-orchestration-mode.md`、`.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/webview/graphRunPanel.ts`、`src/test/graphRunPanel.test.ts` | `npm run build`；`node --test dist/test/graphRunPanel.test.js` 19/19 | GraphRunPanel 默认 LR，但内部支持 `LR` / `RL` / `TB` / `BT` 方向；使用目标系统 workflow 画布的 dagre + fallback / collision / 端口 / 视口经验，包含调优间距、多根零入度 fallback、`review_feedback` 与上游 `if_fail` non-ranking 回边、方向感知 collision、端口分离，以及 selected/running/sleeping/blocked/failed 初始居中。该升级仅优化当前 run 可视表达，不改变 DAG 结构或调度语义 |
| AI 对话 / 记忆 | Harness 骨架开关与踩坑记录 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`docs/LONG_TERM_MEMORY_DESIGN.md` | 工具设置、prompt 构建、当前工作区 `.ch/.agents` harness scaffold、`AGENTS.md`、`CLAUDE.md`、`.gitignore` 的 `.codegraph/` 忽略项、`ARCHITECTURE.md` AI 初始化任务、`.ch/docs/memory/`、`.ch/docs/runbooks/PITFALLS.md`、CodeGraph 终端初始化与“安装 CodeGraph”按钮 | `npm run build`；`node --test dist/test/toolSettings.test.js dist/test/memoryRuntimeGate.test.js dist/test/longTermMemory.test.js dist/test/cliPageStaticRenderCoverage.test.js dist/test/clipagescriptruntimecoverage.test.js dist/test/sessionMessageHandlersCoreCoverage.test.js` | 默认关闭；开启时先确认，确认后补齐工作区 harness scaffold、确保 `.codegraph/` 被 git 忽略，并启动最新版 CodeGraph 安装、Codex MCP 注册和当前工作区索引初始化；工作区页独立“安装 CodeGraph”按钮可单独执行同一 CodeGraph 安装/升级链路，Windows 使用 `cmd.exe`、macOS 使用默认 shell；收尾阶段可二次确认并复用当前 AI 对话以 coding 模式初始化 `ARCHITECTURE.md`；`PITFALLS.md` 记录带根因/规避/验证线索的失败、阻塞、回滚或明确踩坑总结；关闭后只允许查看/导出/删除，不控制 Codex / Claude / OpenCode 外部 CLI 自带记忆或历史 |
| AI 对话 / Codex / OpenCode | 全局隐式子代理开关与子代理进度气泡 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md`、`.ch/docs/exec-plans/completed/2026-07-13-unified-codex-opencode-multi-agent-toggle.md` | 全局工具设置、`src/toolSettings.ts`、`src/workspaceSettingsStore.ts`、`src/sessionMessageActions.ts`、`src/interactive/codexRunner.ts`、`src/interactive/codexRunnerRuntime.ts`、`src/cli/opencodeconfigmodels.ts`、`src/cli/opencoderuntimeconfig.ts`、`src/extension.ts`、`src/subagentProgress.ts`、Webview 消息路由 | `npm run build`；`node --test dist/test/toolSettings.test.js dist/test/codexRunnerRuntime.test.js dist/test/opencodeconfigmodels.test.js dist/test/opencoderuntimeconfig.test.js dist/test/workspaceSettingsStore.test.js dist/test/sessionMessageActions.test.js dist/test/multiAgentSettingWebview.test.js` | 工具设置全局页展示“隐式子代理”，`multiAgentEnabled` 保存在 `~/.sinitek_cli/settings.json`，默认关闭；旧工作区 `multiAgentEnabled` / `codexMultiAgentEnabled` 仅作为迁移输入，全局值优先。关闭时 Codex app-server 显式禁用 `multi_agent`；OpenCode 本次 `OPENCODE_CONFIG` 临时 overlay 合并顶层 `permission.task="deny"`，并以更高优先级的 `OPENCODE_CONFIG_CONTENT` 内联覆盖重申该拒绝，因此项目配置不能重新放开子代理；两者都不写回用户配置。开启时撤销插件侧禁用并保留 CLI 自身可用的隐式子代理能力与 OpenCode task 权限。该开关不控制 Loop 的 `main_sub_multi_agent` / `debate_multi_agent` 编排。Codex 子代理仍按 App Server `threadId` 分流独立 assistant 气泡，并发输出不会合并，子线程不会覆盖/结束父线程，子代理文本不参与父任务 final-answer 判定 |
| AI 对话 / 通用 | 最终答复固定严格协议 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/finalAnswerProtocol.ts`、`src/finalConclusion.ts`、共享 prompt 构建、Codex app-server 回合事件适配、OpenCode JSONL `stop` 终态关联、最终结论气泡判定 | `npm run build`；`node --test dist/test/finalAnswerPolicy.test.js dist/test/promptRuntime.test.js dist/test/finalConclusion.test.js dist/test/codexRunnerRuntime.test.js dist/test/opencodeCommandRunner.test.js dist/test/toolSettings.test.js dist/test/sessionMessageActions.test.js` | 普通任务固定使用严格协议，没有用户可切换的兼容回退：结构化 final 类型优先；Codex 接受 `phase="final_answer"`，OpenCode 接受同一 `messageID` 的非 thinking assistant `text` + `step_finish.reason="stop"`。没有结构化类型时，Codex / Claude / OpenCode 仅接受当前用户消息之后包含 `[final_answer]` 的非 thinking assistant 文本。普通任务 prompt 和 hidden retry prompt 都要求模型仅在完成任务后的最终回复以该标记开头；Loop 等内部机器协议继续使用自身结构化终态，不注入或要求文本标记。成功退出或 Codex commentary-only `turn.completed` 不会合成为最终答复；遗留 `finalAnswerPolicy` / `codexFinalAnswerPolicy` 设置被忽略 |
| AI 对话 / 通用 | 最终答复协议标记展示过滤 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | Webview assistant 气泡渲染 | `npm run build`；`node --test dist/test/finalAnswerPolicy.test.js` | 仅从 assistant 气泡的展示文本移除 `[final_answer]`；不修改用于严格判定、会话续接和存档的原始消息，user 气泡保持原样 |
| AI 对话 / Codex | 推理摘要空注释精准清洗 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/codexReasoningContent.ts`、Codex app-server reasoning 解析、Codex thinking 会话清洗 | `npm run build`；`node --test dist/test/codexReasoningContent.test.js dist/test/codexAppServerProtocol.test.js dist/test/codexRunnerRuntime.test.js` | 仅从 Codex reasoning/thinking 内容移除独占一行的空 HTML 注释 `<!-- -->`（含水平空白变体）；普通 assistant/user 内容、行内空注释和非空 HTML 注释保持原样；加载历史 Codex 会话时同步清理已落盘残片 |
| AI 对话 / 通用 | 内部思考 wrapper 定向清洗 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/thinkingMarkup.ts`、OpenCode JSONL text/reasoning 解析、Codex reasoning 与历史消息清洗 | `npm run build`；`node --test dist/test/opencodeCommandRunner.test.js dist/test/codexReasoningContent.test.js` | 仅识别 `<thinking>`、`<think>`、`<analysis>`、`<reasoning>` 四类内部 wrapper；混合 text 事件拆成 thinking 与 assistant，最终正文排除思考块，历史消息去除 wrapper；普通 `<div>` 等 HTML/代码标签保持原样 |
| AI 对话 / OpenCode | `todowrite` 任务列表与工具标题本地化 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/cli/openCodeTaskList.ts`、`src/cli/commandRunner.ts`、`src/openCodeTabStream.ts`、`src/extension.ts`、`src/webview/viewContentHtml.ts`、`src/webview/viewContentStyles/tasklist.ts`、`src/webview/viewContentScript/taskListAndUi.ts`、`src/webview/viewContentScript/coreRuntimeState.ts`、`src/webview/viewContentScript/traceRendering.ts`、`src/webview/viewContentI18n.ts` | `npm run build`；`node --test dist/test/openCodeTaskList.test.js dist/test/openCodeTabStream.test.js dist/test/openCodeTaskListOverlay.test.js dist/test/opencodeCommandRunner.test.js dist/test/clipagescriptruntimecoverage.test.js dist/test/traceToolTitleLocalization.test.js` | OpenCode 从 JSONL `tool_use/todowrite` 的 `state.input.todos`（兼容 metadata/output）实时更新当前或并行 conversation tab 的任务列表，空数组清空；每次新 AI run 启动会先向目标 tab 发送空 `taskListUpdate` 并清除重放缓存，避免旧列表跨任务残留；专用 `taskListUpdate` 与同一 tool trace 的任务元数据形成双通道，Webview 重载时会重放运行中列表，运行中的消息刷新不再清空浮层。任务列表标题有收起/展开箭头，收起后仅保留标题、进度和箭头，进度按已完成/总数显示，例如 `2/4`；常见 tool-use 气泡标题按中英文界面本地化，未知工具与详情正文保持原样 |
| AI 对话 / Codex | assistant `Tasklist` 任务列表解析与气泡过滤 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/promptRuntime.ts`、`src/interactive/codexAppServerProtocol.ts`、`src/extension.ts`、`src/webview/viewContentScript/taskListAndUi.ts`、`src/webview/viewContentScript/messageRendering.ts`、`src/webview/viewContentScript/traceRendering.ts` | `npm run build`；`node --test dist/test/promptRuntime.test.js dist/test/codexAppServerProtocol.test.js dist/test/clipagescriptruntimecoverage.test.js` | 普通 Codex 人类对话 prompt 会注入 TaskList logging requirement，要求进度更新统一使用 `Tasklist:` 标题和逐行 `- [pending]` / `- [in_progress]` / `- [completed]` 状态，任务描述默认中文表达且代码符号、命令、路径、包名和用户原文术语可保留原文；新 run 的 `runStatus:start` 会按目标 tab 先重置 taskList 起点并隐藏旧列表；Codex app-server assistant 消息仍从实际日志里的 `Tasklist:` / `Tasklist update:` / `Tasklist 更新：` 段落解析任务，兼容多行、checkbox、中文状态词和同行分号分隔；已解析的纯任务列表 assistant 气泡不再进入对话，普通说明混合 Tasklist 时仅剥离任务列表片段并保留正文 |
| AI 对话 / 通用 | Vibe 人工交互表单 | active | 终端用户 | `.ch/docs/exec-plans/completed/2026-08-08-human-interaction-vibe.md`、`.ch/docs/exec-plans/completed/2026-08-08-human-interaction-natural-clarification.md`、`.ch/docs/design-docs/vscode-cli-extension-runtime.md` | 全局工具设置、`src/humanInteraction.ts`、`src/interactive/codexRunner.ts`、`src/extensionHost/promptInteractiveRuntime.ts`、`src/extensionHost/promptOneShotRuntime.ts`、`src/extensionHost/promptParallelRuntime.ts`、`src/promptRuntime.ts`、`src/extension.ts`、`src/webview/viewContentHtml.ts`、`src/webview/viewContentScript/settingsAndOverlays.ts`、`src/webview/viewContentScript/windowMessageDispatch.ts` | `npm run build`；`node --test dist/test/humanInteraction.test.js dist/test/promptRuntime.test.js dist/test/promptInteractiveRuntime.test.js dist/test/promptOneShotRuntime.test.js dist/test/extensionHostExtractionContracts.test.js dist/test/toolSettings.test.js dist/test/sessionMessageActions.test.js dist/test/multiAgentSettingWebview.test.js` | `humanInteractionEnabled` 保存到 `~/.sinitek_cli/settings.json`，默认开启，并在工具设置全局页展示“人工交互”开关。开启后 Codex Vibe/coding 交互任务可拦截 Codex app-server `item/tool/requestUserInput` 与 MCP `mcpServer/elicitation/request` 结构化请求；若用户原始 prompt 明确要求 AI 先询问需求/细节，而 Codex、Claude 或 OpenCode 只输出普通问题列表，运行时会移除该问题列表 assistant 气泡并兜底打开同一人工交互表单，且把问题文案中的“可选 / 选项 / 例如 / 如”候选项和紧随问题的 `A.` / `B.` / `C.` 字母选项列表解析为 radio/checkbox 字段；提交后把补充信息作为用户消息写入当前会话并继续同一 Codex thread、Claude session 或 OpenCode one-shot/parallel 会话；Loop/Graph 不复用该弹窗。Webview 使用当前 overlay/modal 主题变量渲染 text、password、textarea、radio、checkbox、select、multiselect 表单；结构化请求提交后把 answers/content 返回给 Codex 继续执行；拒绝或关闭表单会追加拒绝说明、终止当前任务并关闭弹窗，不进入 hidden retry |
| AI 对话 / Loop | 独立子任务主对话实时进度气泡 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/loopSubtaskProgress.ts`、`src/subagentProgress.ts`、`src/extension.ts`、Loop 主/子 conversation tab 消息存储与 Webview 定向消息协议 | `npm run build`；`node --test dist/test/loopSubtaskProgress.test.js dist/test/subagentProgress.test.js dist/test/loopSubtaskLifecycle.test.js dist/test/finalConclusion.test.js` | 每个 Loop 独立子任务启动后，主任务对话立即生成一个带子任务标题和执行中状态的 assistant 气泡；子任务 tab 的非 thinking、非内部子代理 assistant 快照每秒同步到该气泡，完成、失败或中断时原位收口。子任务自己的 assistant/thinking/trace 气泡继续按 tab 展示；主任务中的 Loop 进度气泡带稳定 `subagentId`，不参与父任务 final-answer 判定 |
| AI 对话 / OpenCode | 子代理事件同步与 60 秒补捞气泡 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/cli/openCodeSubagentMonitor.ts`、`src/cli/commandRunner.ts`、`src/subagentProgress.ts`、`src/extension.ts`、`src/i18n.ts`、one-shot / 并行 tab / Webview 消息路由 | `npm run build`；`node --test dist/test/openCodeSubagentMonitor.test.js dist/test/subagentProgress.test.js dist/test/finalConclusion.test.js dist/test/openCodeTabStream.test.js dist/test/opencodeCommandRunner.test.js` | OpenCode 父 `run --format json` 不转发内部子代理增量时，插件显式启动受管 `opencode serve`、等待健康后再以 `run --attach` 执行父任务；SSE 事件触发子会话消息快照刷新，并每 60 秒通过公开 children/message/status API 全量补捞。每个当前尝试新建的子 session 固定更新独立 assistant 气泡；任务结束统一关闭服务、订阅和轮询。服务不可用时只显示一次降级状态并继续父任务，SSE 重连使用最长 60 秒的指数退避；不读取私有 SQLite，子代理气泡不参与父任务 final-answer 判定 |
| AI 对话 / OpenCode | 当前工作区 cwd/PWD 一致执行 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/cli/commandRunner.ts`、`src/extension.ts` | `npm run build`；`node --test dist/test/opencodeCommandRunner.test.js` | OpenCode one-shot、并行和 Loop 子任务以当前 VS Code 工作区为 spawn cwd，并把 child env `PWD` 强制同步为同一路径；即使 extension host 继承 `PWD=/`，OpenCode `run` 内部请求也不会再把新会话创建到文件系统根目录或要求用户重新选择项目；修复前已绑定 `/` 的历史会话需新建一次对话，不直接篡改 OpenCode 数据库 |
| AI 对话 / 会话 | 历史提示词收藏与仅收藏过滤 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/exec-plans/completed/2026-08-09-prompt-history-favorites.md` | `src/promptHistoryStore.ts`、`src/extensionHost/modelSettings.ts`、`src/sessionMessageHandlers.ts`、`src/webview/viewContentHtml.ts`、`src/webview/viewContentScript/historyPanels.ts`、`src/webview/viewContentStyles/overlaysModals.ts` | `npm run build`；`node --test dist/test/promptHistoryStore.test.js dist/test/sessionMessageHandlersCoreCoverage.test.js dist/test/clipagescriptruntimecoverage.test.js` | 历史记录弹窗“历史提示词”页签每条提示词提供星标收藏按钮，收藏状态持久化到 prompt history 记录的 `favorite` 字段；旧记录缺失字段时按未收藏读取。页签顶部提供“仅收藏”过滤和收藏计数，过滤状态保存在 Webview 本地 UI 状态，不写入全局或工作区设置；过滤后无结果显示“暂无收藏提示词/No favorite prompts”，清空提示词仅清空未收藏项并保留所有收藏提示词；30 天 retention 和数量上限也只裁剪未收藏提示词 |
| AI 对话 / 会话 | 当前会话提示词倒序查看 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | 对话运行状态区“提示词”按钮、当前会话消息运行态、提示词弹层 | `npm run build`；`node --test dist/test/runPromptHistoryWebview.test.js` | 仅展示当前 tab 会话的非空用户输入；按 `createdAt` 倒序，缺少时间时按消息顺序倒序；最新提示词置顶 |
| AI 对话 / 会话 | 会话 Tab 分页边界按钮 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/webview/viewContentScript/messageRendering.ts`、`src/webview/viewContentStyles/headerTabs.ts` | `npm run build`；`node --test dist/test/conversationTabPagination.test.js` | 超过 5 个 tab 时每页最多显示 5 个；第一页只显示“下一页”，中间页显示两个方向，最后一页只显示“上一页”；不可用方向直接隐藏，不渲染为 disabled 按钮 |
| AI 对话 / 会话 | 历史会话 Loop/Graph 标记与统一恢复入口 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | 历史记录“历史会话”列表、Loop 任务与会话绑定、Graph run 与会话/消息绑定、输入区“打开群聊”按钮、底部“打开 Graph 图”按钮 | `npm run build`；`npm run test:page`；`node --test dist/test/graphMainWebview.test.js dist/test/graphStore.test.js` | 通过 Loop 任务持久化的 `cli + sessionId` 精确标记 Loop 会话；通过 Graph run store 的 `cli + sessionId` 或已保存消息中的 `graphRunId` / `openGraphRun` action 标记 Graph 会话。移除重复的“Loop 群聊”恢复 tab，加载会话后使用现有“打开群聊”按钮进入群聊；加载 Graph 会话后使用底部“打开 Graph 图”按钮进入运行图，后续普通用户回复不会清除当前 Graph run 入口 |
| AI 对话 / 配置中心 | 三组 CLI 可视化配置、参数提示与范例入口 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/exec-plans/active/2026-07-12-cli-config-visualization.md`、`.ch/docs/exec-plans/completed/2026-07-31-codex-env-removal-keep-visual-editor.md`、`.ch/docs/references/cli-runtime-reference.md` | Claude / OpenCode / Codex 配置卡片可视化参数、配置文件名区域、配置页诊断说明 | `npm run build`；`node --test dist/test/codexConfigVisualEditor.test.js dist/test/claudeConfigVisualEditor.test.js dist/test/opencodeconfigvisualeditor.test.js`；raw value/排序定向 `dist/test`；`node --check media/config/assets/config-app-ui.js` | 三组可视化参数 label 右侧提供问号 tooltip，枚举提示列出当前候选，三组“查看范例”统一位于配置文件名右侧。Codex 保留 `config.toml` 可视化编辑和 TOML 源码模式，Provider 可视化不提供 `env_key` 输入；不展示、读取、写入、备份或导入导出 `.env`，也不删除用户已有文件，历史 `envContent` 在档案中忽略。Claude 将 `permissions.defaultMode` 升级为完整单选，并新增稳定布尔/行为字段；OpenCode 新增顶层 `share`/`autoupdate`/`logLevel`/`snapshot` 受控字段，`model`/`small_model`/`npm` 改为可编辑组合框，reasoning effort 改为动态可输入 tags。点击配置档案默认进入源码模式（Claude/OpenCode 为 JSON，Codex 为 TOML），三组均保留原生 TOML/JSON 源码、未知值和复杂配置保护；`AugmentExtensionSidecar` 403 仍按外部扩展侧请求失败口径处理 |
| AI 对话 / OpenCode | OpenCode CLI、会话续接与配置驱动主/子模型 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | AI 对话 CLI 分组、OpenCode JSONL `sessionID` 提取、tab 会话接管、active config 模型解析、主模型/子模型下拉、按配置隔离的角色覆盖、运行时 config overlay、运行时 profile 日志、会话存档 | `npm run build`；`node --test dist/test/opencodeCommandRunner.test.js dist/test/opencodedualmodelwebview.test.js`；OpenCode parser/Webview/runner/runtime/store/config example tests | 首轮接管真实 `ses_*`，后续同 tab 通过 `--session` 续接；`local_*` 只用于插件消息暂存且不会传给 CLI，旧占位会话捕获新真实 ID后迁移消息和 tab 引用；候选只从 active config 的 `provider.<id>.models` 加载且无管理入口；聊天区 OpenCode 在 Loop/Graph 模式按两行展示主模型与子模型及各自思考力度，Vibe/coding 仅展示主模型与主模型思考力度，底层 `model` / `small_model` 仅作为 OpenCode CLI 配置字段适配；正常 option 只显示 `models.<id>.name`（缺失时回退 model id）；选择配置默认 ref 会清除临时覆盖，其他项发送 exact ref；Vibe 和普通并行任务使用 effective main，Loop 主任务、主持/复核、续跑和唤醒使用 main，Loop 子任务使用 subtask，Graph planner 和最终 `summary` 节点使用 main，Graph 其他执行节点使用 subtask；Vibe 不读取或覆盖 `small_model`，Loop/Graph 子模型和两套思考力度才通过随机 `OPENCODE_CONFIG` overlay 注入；`opencode-runtime-profile` 及启动日志记录 main/subtask model 与两套 variant，Vibe 的 subtask 值为空；one-shot 只在启动后 60 秒完全无 activity 时超时，首个有效事件后不再以父 JSONL 静默判断卡死，避免分组/子代理任务误杀；覆盖按 config id 隔离并清理失效值，overlay 权限为目录 `0700`/文件 `0600`，exit/error/timeout/cancel 后清理且不改写用户配置 |
| AI 对话 / OpenCode | coding / Loop / Graph 模式入口 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/design-docs/vscode-cli-extension-runtime.md`、`.ch/docs/design-docs/graph-orchestration-mode.md` | 对话面板模式选择器、`src/sessionMessageActions.ts`、Loop 主任务/子任务/多轮复核/群聊编排、Graph `modelRouting`、active config effective main/subtask 运行链路 | `npm run build`；OpenCode Loop/Graph 模式入口与路由回归测试 | OpenCode 可在对话面板选择 coding、Loop 或 Graph；Loop 复用既有主任务、子任务、多轮和群聊链路，Graph 复用 realized DAG 编排链路，均通过非交互式 one-shot `opencode run` 执行。该能力不表示 `isInteractiveSupported(opencode)=true`，不启用 Codex/Claude interactive runner 或 common command；OpenCode 使用主模型/子模型角色执行 Loop 与 Graph，底层 `model` / `small_model` 仅作为 CLI 配置字段适配；Loop 多智能体执行模式下拉统一位于输入区底部操作图标左侧，与 Codex/Claude 布局一致；同一 tab 保留 Graph 图入口/元数据时，前台新发送仍跟随当前 UI 模式，切到 coding/Vibe 或 Loop 后复用会话上下文但不被自动路由回 Graph，后台队列和 Graph 续跑才允许按 Graph tab 元数据自动归入 Graph |
| AI 对话 / Loop/Graph | Codex/OpenCode 主/子模型路由与 CLI 模型职责 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md`、`.ch/docs/design-docs/vscode-cli-extension-runtime.md`、`.ch/docs/design-docs/graph-orchestration-mode.md` | Webview 模型区域、`sendPrompt.model`、`loopMainModel`、`loopSubtaskModel`、`loopMainThinkingMode`、`loopSubtaskThinkingMode`、OpenCode main/subtask 状态、Loop 普通轮次/辩论/续跑/唤醒运行链路、Graph `modelRouting` / 节点 `modelRole` 记录、模型状态兼容读取 | `npm run build`；`node --test dist/test/codexdualmodelwebview.test.js dist/test/sessionMessageActions.test.js dist/test/graphExtensionRuntime.test.js dist/test/loopPromptQueue.test.js dist/test/opencodedualmodelwebview.test.js dist/test/opencodeloopmodewebview.test.js dist/test/opencoderolemodelruntime.test.js` | Claude 在 Coding/Loop 均不显示插件侧模型选择；Codex 普通 Coding 保持单模型，切到 Loop 或 Graph 时显示主模型/子模型两个选择器，并对 Codex 两个角色分别显示与保存思考力度；OpenCode 在 Loop/Graph 中显示主模型/子模型两个角色，Vibe/Coding 仅显示并使用主模型，底层 `model` / `small_model` 仅作为 OpenCode CLI 配置字段适配。Loop 主任务、主持/复核、续跑与自动唤醒使用主模型及主任务思考力度，Loop 子任务使用子模型及子任务思考力度；Graph planner 和最终 `summary` 节点使用主模型及主任务思考力度，Graph 其他执行节点使用子模型及子任务思考力度；Vibe 不读取或覆盖子模型，缺少子模型不阻断 Vibe；Loop/Graph 缺少子模型时兼容回退到主模型或单模型并记录原因。群聊显式继续以主任务 Tab 当前 CLI 分组为权威，读取该 CLI 当前激活配置和模型选择，并在跨 CLI 时迁移同一任务 ID 的 CLI/session Store 归属；legacy lobster 与旧 Loop 主/子模型、OpenCode 旧 `primary` / `small` 字段仅用于兼容读取和迁移 |
| AI 对话 / Loop | 子任务思考力度全局上限 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md`、`.ch/docs/exec-plans/completed/2026-07-15-loop-subtask-thinking-cap.md` | 工具设置全局页、`src/loopSubtaskThinking.ts`、`src/toolSettings.ts`、`src/sessionMessageActions.ts`、`src/panelStateBuilder.ts`、`src/extension.ts` | `npm run build`；`node --test dist/test/loopSubtaskThinking.test.js dist/test/toolSettings.test.js dist/test/opencodethinkingintegration.test.js` | `loopSubtaskMaxThinkingMode` 保存到 `~/.sinitek_cli/settings.json`，默认 `xhigh`，可降低为 `low / medium / high`；每个 Loop 子任务以该上限和当前模型选择中的较低力度运行，`max` / `ultra` 最多为 `xhigh`；不改写主任务、普通任务或模型级用户选择 |
| AI 对话 / Loop | Loop 主任务 Tab 生命周期运行态与队列约束 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/design-docs/vscode-cli-extension-runtime.md` | `src/sessionTabs.ts`、`src/extension.ts`、`src/loopOrchestrationOwnership.ts`、`src/webview/viewContentScript/messageRendering.ts`、`src/webview/viewContentScript/runStreamAndQueue.ts` | `npm run build`；`node --test dist/test/conversationTabLock.test.js dist/test/loopPromptQueue.test.js dist/test/sessionMessageActions.test.js dist/test/loopDebate.test.js dist/test/loopOrchestrationOwnership.test.js` | Loop 主任务 tab 使用 `☀️` 图标、子任务 tab 使用 `🌛` 图标，均不额外拼接 `Loop` 文本；切换 tab 时，只有带有效任务 ID 的 Loop 主任务自动切换到 Loop 模式，Loop 子任务和普通 tab 均自动切换到 Vibe；任务记录仍为 `running` 时，主 Tab 持续显示运行态并保持不可关闭，不依赖当前是否存在 AI/CLI 执行进程；`needs-review`、`error`、`stopped` 中断终态优先于异步释放中的旧编排所有权，立即结束视觉运行态并解除锁定，旧编排的轮次/重试派发门禁禁止把任务重新写回 `running`；同一任务并发退出/恢复的所有权按引用计数释放；该 tab 的新提示词直接入队，阶段性进程结束不提前出队；只有任务变为 `completed` 才自动继续队列，中断终态保留队列等待人工继续 |
| AI 对话 / Loop | 可解析任务决策自动睡眠、持久化定时唤醒与群聊倒计时 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/design-docs/vscode-cli-extension-runtime.md`、`.ch/docs/design-docs/loop-debate-multi-agent-mode.md` | `src/loopAutoWake.ts`、`src/loopTaskStore.ts`、`src/extension.ts`、`src/panelStateBuilder.ts`、`src/webview/loopDebatePanel.ts` | `npm run build`；`node --test dist/test/loopAutoWake.test.js dist/test/loopTaskStoreCoreCoverage.test.js dist/test/loopDebate.test.js dist/test/loopPromptBuilders.test.js dist/test/loopDebatePanel.test.js` | 任何由宿主解析本 JSON 决策协议的 Loop 任务都可返回 `status=sleep`、整数 `wakeAfterSeconds` 和 `sleepReason`；普通自由文本回复不会触发自动睡眠。扩展持久化 `sleeping + autoWakeAt` 后释放当前 CLI 调用；到期复用原任务/session 和当前轮次自动继续，扩展重启后恢复，长等待分段定时，VS Code 退出期间不运行外部守护进程；合法睡眠任务不会被普通历史清理提前删除，人工继续/中止会取消陈旧唤醒；AI 对话显示可读睡眠 assistant 气泡，Loop 群聊显示每秒倒计时、计划唤醒时间、原因及睡眠气泡，并允许提前继续或中止取消唤醒 |
| AI 对话 / Loop | Loop 群聊用户任务与发言气泡 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/loopDebate.ts`、`src/panelDiagnostics.ts`、`src/webview/loopDebatePanel.ts`、`src/webview/loopDebatePanelRenderer.ts`、`src/webview/loopDebatePanelStyles.ts` | `npm run build`；`node --test dist/test/loopDebate.test.js dist/test/loopDebatePanel.test.js` | 创建或恢复任务时，任务记录中的原始 `rootPrompt` 会作为最顶部、标记为“任务发起”的右侧“我”气泡展示，不重复写入群聊 transcript；未完成且可补充需求的任务显示“我要说话”，提交内容写入任务记录和主通信文件，刷新后按提交顺序继续显示右侧“我”的对话气泡，并隐藏内部时间和轮次元数据 |
| AI 对话 / OpenCode | 精确主/子模型动态推理 variant | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/cli/openCodeModelCapabilities.ts`、`src/modelSelectionStore.ts`、`src/panelStateBuilder.ts`、`src/extension.ts`、`src/cli/commandRunner.ts`、Webview thinking selector | `npm run build`；相关 OpenCode resolver/config/Webview/runner/action/integration tests | Loop/Graph 的主模型与子模型思考力度分别来自对应精确模型 CLI metadata 或当前配置显式声明；Vibe/Coding 仅应用主模型思考力度。主模型可用 `--variant`，子模型因 CLI 无 `--small-variant`，仅在 Loop/Graph 由 runtime config overlay 的 `small_model` 兼容字段写入对应模型 `options.reasoningEffort`；未知模型 Default-only，不按 npm/provider/model 名猜测，`--thinking` 仅控制 thinking blocks |
| AI 对话 / Gemini | Gemini CLI 支持下线 | removed | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | 旧 AI 对话 Gemini 分组、旧配置中心 Gemini 卡片、旧 `geminiStreamJson` / catalog 路径 | 文档校验：`rg -n "Gemini\|gemini" <授权文档>`；源码验证由对应实现子任务负责 | Gemini 已从当前 AI 对话和配置中心支持范围移除；官方 Skills catalog 条目、内置 ZIP 和 legacy 同步入口也已删除，历史只通过 Git 与归档文档审计 |
| AI 对话 / 上下文 | 全局执行后自动压缩上下文 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md`、`.ch/docs/exec-plans/completed/2026-07-13-global-auto-compact-setting.md` | 全局工具设置、`src/toolSettings.ts`、`src/workspaceSettingsStore.ts`、`src/sessionMessageActions.ts`、`src/contextCompactionRunner.ts`、`src/extension.ts`、Codex/Claude/OpenCode interactive runner 或 OpenCode 非交互 slash command fallback | `npm run build`；`node --test dist/test/toolSettings.test.js dist/test/workspaceSettingsStore.test.js dist/test/sessionMessageActions.test.js dist/test/multiAgentSettingWebview.test.js dist/test/contextCompactionRunner.test.js dist/test/opencodethinkingintegration.test.js` | `autoCompactContextAfterRun` 保存在 `~/.sinitek_cli/settings.json`，默认开启；旧工作区 after-run/before-run 字段仅作为迁移输入，全局值优先，成功迁移或用户更新后移除当前工作区旧字段。仅成功结束且执行超过 5 分钟的已有会话触发；自动压缩为静默后台任务，不追加普通任务完成耗时气泡、不覆盖刚完成任务的真实执行时间；手动压缩仍显示压缩运行状态；OpenCode 官方 TUI slash commands 支持 `/compact`（alias `/summarize`）用于 compact current session，配置 `compaction.auto` 默认开启，插件侧可复用当前实现链路或在非交互受限时 fallback 发送官方 slash command |
| 配置 / 配置中心 | CLI 配置档案管理与卡片级保存 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/exec-plans/active/2026-07-12-cli-config-visualization.md`、`.ch/docs/exec-plans/completed/2026-07-31-codex-env-removal-keep-visual-editor.md`、`.ch/docs/exec-plans/completed/2026-07-31-vscode-config-fullscreen-panel.md`、`.ch/docs/exec-plans/active/2026-07-31-vscode-config-zen-mode-popup.md` | 前台聚焦的 VS Code `WebviewPanel`、Zen Mode 宿主、`media/config/assets/config-app-ui.js`、`src/config/configService.ts`、`src/webview/configPanel.ts`、`src/webview/configView.ts` | `npm run build`；`node --check media/config/assets/config-app-ui.js`；`node --test dist/test/configService.test.js dist/test/cliPageConfigCoverage.test.js dist/test/codexConfigVisualEditor.test.js dist/test/claudeConfigVisualEditor.test.js dist/test/opencodeconfigvisualeditor.test.js` | 从 AI 对话面板点击“配置”立即打开并前台聚焦 VS Code 编辑器主区域的配置面板，并尝试进入 Zen Mode 隐藏工作台外围以接近全屏弹窗；重复点击复用同一面板且不重复切换，关闭后恢复由本插件进入的 Zen Mode，命令不可用时回退为普通编辑器面板。VS Code 扩展公开 API 不提供任意 Webview 的原生模态弹窗，因此不启动独立浏览器或本地 HTTP 页面。面板继续支持配置档案列表、排序、激活、删除、初始化、备份、导出、Skills 和 MCP 管理，点击或新增选择配置档案时默认打开源码模式。配置页布局和交互尽量保持携宁 CLI 配置页一致，同时铺满 WebviewPanel 剩余视口，大型配置浮层使用近满屏宽高以减少左右和底部空白；颜色、字体、边框、焦点、状态、弹层和背景全部映射到 VS Code `--vscode-*` 语义变量，并在 VS Code 主题切换时重新解析 Ant Design token。Codex 卡片管理 `~/.codex/config.toml` 和既有 `auth.json` 入口，保持 TOML 可视化/源码模式，Provider 可视化不提供 `env_key` 输入，受控单选、布尔和多行文本仅定向写入已管理字段，复杂 TOML 与 legacy/未知值保留；不展示、读取、写入、备份或导入导出 `.env`，也不删除用户已有文件。Claude 卡片管理用户级 `~/.claude/settings.json`，保持 JSON 可视化/源码模式；稳定 boolean、行为单选、完整 `permissions.defaultMode`、模型/网关自由文本和可编辑列表并存，额外 `env`、hooks、复杂权限和未知字段不丢失。OpenCode 模型/Provider 卡片只维护 `~/.opencode/config.json`，全局 MCP 市场单独维护 `${XDG_CONFIG_HOME:-~/.config}/opencode/opencode.json` 顶层 `mcp`，不提供 `.env` 第二保存入口；其顶层稳定字段使用继承语义控件，动态 model/npm 使用可编辑组合框，reasoning effort 使用动态可输入多值 tags，未知顶层/provider/model/options/复杂 variants 与无效 JSON 保护保持。思考力度显示 raw value；Codex 固定新建值为 `minimal / low / medium / high / xhigh / max / ultra`，`max` 紧跟 `xhigh`；Claude 固定新建值以 `ultra` 结束，存量 `max` 仅兼容显示在 `ultra` 前；OpenCode 精确 provider/model 动态 variants 保持 payload 原值和顺序 |
| 配置 / MCP | OpenCode 官方全局配置文件 MCP 管理 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/config/openCodeMcpConfig.ts`、`src/config/mcpService.ts`、MCP 市场、`media/mcp_marketplace.json`、`scripts/validate_mcp_marketplace.js` | `npm run validate:mcp-marketplace`；`npm run build`；`node --test dist/test/openCodeMcpConfig.test.js dist/test/configMcpService.test.js dist/test/opencodeMcpHealth.test.js dist/test/mcpInstalledStatus.test.js dist/test/mcpMarketplaceCatalog.test.js` | 安装和卸载直接管理 `${XDG_CONFIG_HOME:-~/.config}/opencode/opencode.json` 顶层 `mcp`；local 使用命令数组与 `environment`，remote 使用 URL 与 `headers`；安装保留其他顶层字段和已有 MCP，卸载只删除目标键且幂等；JSON/JSONC 解析失败不覆盖原文件，成功修改原子写回；MCP 市场打开时仅从 Claude / Codex / OpenCode 各自配置文件快速读取安装状态，健康检测由按钮单独触发，`opencode mcp list --pure` 仅用于 OpenCode 健康检测；内置市场已压缩刷新为 GitHub、Microsoft Learn、Playwright、Docker Gateway、Cloudflare、Stripe、Sentry、MongoDB、Grafana、Elastic、Slack、Notion、Linear、Brave、Atlassian 等官方/权威候选，校验脚本阻止旧 `modelcontextprotocol/servers/tree/main/src/` 路径和旧 `@modelcontextprotocol/server-*` 包名回流 |
| 配置 / 配置中心 | 窄屏配置目录初始展开 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | 对话面板配置按钮、`media/config/assets/config-app-ui.js`、配置中心 `920px` 响应式布局 | `npm run build`；`node --check media/config/assets/config-app-ui.js`；`node --test dist/test/configappcompactcontrols.test.js` | 从配置按钮首次打开配置中心且当前视口小于等于 `920px` 时，左侧配置目录默认展开；用户仍可通过关闭按钮、遮罩或 `Esc` 收起，宽屏初始状态不变 |
| AI 对话 / Loop | Loop 命名与本地存储迁移 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md`、`.ch/docs/design-docs/vscode-cli-extension-runtime.md` | `src/loopLegacyMigration.ts`、`src/loopTaskStore.ts`、设置/模型/任务运行/会话消息读取边界、`sinitek-cli-tools.openLoopGroupChat` | `npm run build`；`node --test dist/test/loopLegacyMigration.test.js dist/test/loopTaskStore.test.js dist/test/toolSettings.test.js dist/test/workspaceSettingsStore.test.js dist/test/opencoderolemodelruntime.test.js dist/test/codexReasoningContent.test.js` | 新任务和通信 artifact 只写入 `loop-tasks/.../loop-tasks.json` 与 `loop-communications/<taskId>/`；升级后首次任务枚举会迁移旧 Lobster 目录与记录，持久化旧前缀键按新键优先归一化；群聊公开命令改为 `openLoopGroupChat`，旧命令仅保留隐藏兼容别名 |
