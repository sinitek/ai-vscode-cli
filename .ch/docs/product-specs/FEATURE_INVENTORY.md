# 功能总表

这个文件是当前仓库功能清单的**单一事实来源**。

作为 starter，这里默认从空表开始；当首个真实能力进入规划或实现时，再新增条目。

工具设置当前 UI 采用“常规配置”“AI任务配置”“工作区”“自动修复”四个 Tab；历史保留天数归入“常规配置”，隐式子代理、人工交互、执行后自动压缩上下文、Loop 参数及 Loop+ 单次派发上限归入“AI任务配置”，CLI `spawn ENOENT` 归入“自动修复”。

## 更新规则

- 新增能力时新增一行。
- 修改已有能力时更新状态、角色、规格来源、测试状态和备注。
- 下线能力时不要直接删除，改成 `removed` 并保留历史说明。
- 如果一个功能变更无法直接链接到规格，至少要能链接到执行计划或设计文档。
- 不要让 README、聊天记录、任务单、临时表格分别维护不同版本的功能列表。

## 什么时候必须更新

- 新增用户可感知能力、后台能力、运维能力或平台能力。
- 修改已有能力的行为、权限、流程、入口、状态或适用角色。
- 能力进入暂停、废弃、替换、下线状态。
- 需求没有新增页面，但改变了导入导出、通知、审计、报表或批处理能力。

## 状态枚举

- `proposed`：已识别，但尚未进入实施
- `in-progress`：正在建设
- `active`：已经交付且当前生效
- `deprecated`：仍存在，但不建议继续使用
- `removed`：已下线，仅保留历史记录

## 维护边界

- 功能名描述用户或系统能力，不直接使用页面名或接口名。
- 测试状态只回答是否有基础自动化护栏，不替代测试报告。
- 执行计划应说明本次是否需要更新本表；收尾说明应记录实际变更项。

## 当前清单

starter 默认不预置功能项。复制模板后，请从第一个真实能力开始维护下表。

## 后端实现边界备注

- 2026-08-29 后端重构没有新增用户入口或下线能力；下表现有 Graph、OpenCode、配置中心和 MCP 能力状态保持不变。
- Graph 相关能力的当前实现边界包含 `src/graph/graphEdgeSemantics.ts`：scheduler、prompt topology 和 review scope 复用同一 active structural/blocking edge 口径；Retry 与 direct rework 都会清理旧 artifact / failure / execution / acceptance evidence。
- CLI / 配置相关能力的当前实现边界包含 `src/cli/commandResolution.ts`、`src/config/configPaths.ts`、`src/shared/userHomePaths.ts`、`src/shared/atomicWrite.ts` 和 `src/shared/jsonObject.ts`：配置化 CLI command string 统一拆分，用户级 `~` / `%VAR%` 路径在进入文件系统前展开为当前平台原生路径，Codex 配置读取 `CODEX_HOME`，Windows 额外发现官方 Codex bin、`%USERPROFILE%\.local\bin`、scoop shims、Volta 和 WinGet Links，配置原子写在 Windows 上可覆盖已有文件，strict/jsonc JSON object 解析统一复用。
- OpenCode 官方全局 MCP 配置仍由 `${XDG_CONFIG_HOME:-~/.config}/opencode/opencode.json` 顶层 `mcp` 驱动；JSONC 只作为输入兼容，成功修改后仍写回格式化严格 JSON。
- OpenCode 可视化模型表单支持官方 `provider.<id>.models.<id>.limit.context` 上下文 token 上限，保存时校验非负有限数值并保留同一模型 `limit` 下的其他字段；相关配置页回归覆盖见 `src/test/config/opencodeconfigvisualeditor.test.ts`。

| 业务域 | 功能名称 | 状态 | 主要角色 | 规格来源 | 实现位置 | 测试状态 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AI 对话 / 配置 | 工具设置自动修复 CLI ENOENT | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/cli/cliCommandRepair.ts`、`src/sessionMessageHandlers.ts`、`src/webview/viewContentHtml.ts`、`src/webview/viewContentScript/settingsAndOverlays.ts` | `npm run build`；`node --test dist/test/cli/cliCommandRepair.test.js dist/test/webview/cliPageStaticRenderCoverage.test.js dist/test/webview/clipagescriptruntimecoverage.test.js` | 工具设置“自动修复”Tab 用表格列出扩展宿主当前找不到的 Codex/Claude/OpenCode，左侧为 `spawn <command> ENOENT` 摘要，右侧为一键修复。修复读取 Windows 用户和系统 PATH（不依赖 VS Code 启动时的旧环境），跳过 WindowsApps 空桩，命中后把绝对路径写入 `sinitek-cli-tools.commands.<cli>` |
| AI 对话 / 配置与模型 | 模型快照按当前配置隔离 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/runbooks/PITFALLS.md` | `src/webview/viewContentScript/modelAndPanelState.ts` | `npm run build`；`node --test dist/test/webview/clipagescriptruntimecoverage.test.js dist/test/webview/codexdualmodelwebview.test.js dist/test/webview/opencodedualmodelwebview.test.js` | 配置切换和后台刷新时，模型候选、选中值、主/子模型及思考选项只接收与界面选中配置一致的快照；其他配置的延迟非空列表不能覆盖当前列表。保留同配置刷新、空列表、配置删除与切换失败回滚 |
| AI 对话 / 定时任务 | 一次性 AI 对话定时执行与附件管理 | active | 终端用户、扩展运行时 | `.ch/docs/exec-plans/completed/2026-09/2026-09-15-scheduled-prompts.md`、`.ch/docs/exec-plans/completed/2026-09/2026-09-15-scheduled-task-runtime-mode.md`、`.ch/docs/exec-plans/completed/2026-09/2026-09-19-scheduled-task-selected-mode.md`、`.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/scheduledTaskStore.ts`、`src/extension.ts`、`src/sessionMessageHandlers.ts`、`src/webview/panelFileActions.ts`、`src/webview/viewContentHtml.ts`、`src/webview/viewContentScript/*` | `npm run build`；`node --test dist/test/scheduledTaskStore.test.js dist/test/extensionHost/extensionHostExtractionContracts.test.js` 24/24；`node --test dist/test/webview/cliPageStaticRenderCoverage.test.js` 9/9；`node --test dist/test/graph/graphRunPanel.test.js` i18n 对齐通过；`git diff --check` | 发送按钮左侧提供土黄色时钟入口；弹窗默认下一天 00:00，可填写提示词、用与 AI 对话相同的 `interactive-mode-select` 选择 Vibe/Loop/Graph 并添加附件；点击“添加”后弹窗保持打开并清空表单，使用右上角关闭；任务写入 `~/.sinitek_cli/scheduled-tasks.json`，附件写入独立目录；扩展启动和工作区切换会恢复对应工作区任务，到点执行时按任务选中的模式及该模式已保存配置路由，Loop 子模式读取创建时的 Loop 配置，旧任务缺模式字段时回退到目标 CLI 当前模式，运行中任务重启后回到 `pending`，终态历史按全局历史保留策略清理 |
| AI 对话 / 通用 | P0 性能与内存硬化 | active | 终端用户、扩展运行时 | `docs/PERFORMANCE_MEMORY_AUDIT_2026-07-13.md`、`.ch/docs/design-docs/vscode-cli-extension-runtime.md`、`.ch/docs/exec-plans/completed/2026-07/2026-07-31-p0-performance-memory-hardening.md` | `src/extension.ts`、`src/interactive/claudeRunner.ts`、`src/cli/commandRunner.ts`、`src/openCodeTabStream.ts`、`src/webview/viewContentScript/*`、`src/webview/panelFileActions.ts` | `npm run build`；`node --test dist/test/interactive/claudeRunner.test.js dist/test/extensionHost/extensionDeactivateStopAll.test.js dist/test/shared/boundedText.test.js dist/test/cli/commandRunnerCoverage.test.js dist/test/cli/opencodeCommandRunner.test.js dist/test/core/openCodeTabStream.test.js dist/test/webview/clipagescriptruntimecoverage.test.js dist/test/session/sessionMessageHandlersCoreCoverage.test.js` 114/114；`git diff --check` | Claude stop 将 AbortController 传给 SDK；deactivate/reload 统一 stop-all；OpenCode raw stdout/stderr 与 JSONL pending line 有界并使用增量 activity tracker；Run Stream 有记录数/字节预算且 overlay 关闭时不构建完整 DOM；Assistant delta 流式阶段轻量更新；附件上传最多 10 个文件、单文件 20 MiB、不限制总大小，Webview 和 Extension Host 双端校验并提供中英文超限提示 |
| AI 对话 / 通用 | `.sinitek_cli` 历史自动清理保留天数 | active | 终端用户、扩展运行时 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md`、`.ch/docs/exec-plans/completed/2026-09/2026-09-13-configurable-auto-cleanup.md` | `src/toolSettings.ts`、`src/historyRetention.ts`、`src/logger.ts`、`src/extension.ts`、`src/sessionMessageActions.ts`、`src/panelStateBuilder.ts`、`src/webview/viewContentHtml.ts`、`src/webview/viewContentScript/*` | `npm run build`；`node --test dist/test/core/toolSettings.test.js dist/test/session/sessionMessageActionsCoreCoverage.test.js dist/test/webview/cliPageStaticRenderCoverage.test.js dist/test/webview/clipagescriptruntimecoverage.test.js` | 工具设置“常规配置”Tab 提供历史保留天数，`historyRetentionDays` 写入 `~/.sinitek_cli/settings.json`，默认 30 天，范围 1–3650 天；日志、会话、提示词历史、任务记录和 Loop 记录统一读取动态期限，临时目录独立的 1 小时策略保持不变 |
| AI 对话 / 通用 | 计费终态错误立即收口 | active | 终端用户 | `.ch/docs/references/cli-runtime-reference.md`、`.ch/docs/runbooks/PITFALLS.md` | `src/panelDiagnostics.ts`、`src/interactive/codexRunner.ts`、`src/interactive/codexRunnerRuntime.ts`、`src/extension.ts` | `npm run build`；`node --test dist/test/core/panelDiagnostics.test.js dist/test/interactive/codexRunnerRuntime.test.js dist/test/core/hiddenRetry.test.js` | HTTP 402、`Payment Required`、明确积分/余额耗尽不进入 hidden retry，首次失败即保留原始 provider 错误并收口；Codex 可见错误使用结构化 `error` trace，不能作为正常进度清零重试计数。429、网络中断和普通工具 trace 继续沿用既有重试与恢复语义 |
| AI 对话 / OpenCode / Loop | 主任务空响应有界恢复 | active | 终端用户、Loop 主智能体 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/openCodeRunCompletion.ts`、`src/extension.ts`、`src/loopTaskStore.ts` | `npm run build`；`node --test dist/test/core/openCodeRunCompletion.test.js dist/test/cli/opencodeCommandRunner.test.js dist/test/core/hiddenRetry.test.js dist/test/session/sessionMessageActions.test.js dist/test/loop/loopTaskStore.test.js dist/test/loop/loopPromptBuilders.test.js dist/test/extensionHost/promptRuntime.test.js` | OpenCode 进程 `code=0` 但当前尝试没有非 thinking assistant 正文时，one-shot 与 parallel 路径进入既有 hidden retry；Loop 后续轮次只认当前进程尝试的正文，不使用历史主任务 JSON 误判成功。对于无 provider error、已有远端 session 的 Loop 主任务，首次空成功响应会在下一次重试新建 session 并重发完整主提示，随后保留旧历史、改绑 tab 和任务存储到新 session；其它路径不切换。重试耗尽后显示并持久化明确错误，合法当前回复继续进入 `LoopMainDecision` 校验 |
| AI 对话 / Loop / 会话 | Loop 任务 Tab 状态收敛与会话重置一致性 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/design-docs/vscode-cli-extension-runtime.md` | `src/extension.ts`、`src/loopDebate.ts`、`src/sessionTabs.ts`、`src/webview/viewContentScript/messageRendering.ts`、Webview 事件绑定 | `npm run build`；`node --test dist/test/session/conversationTabLock.test.js dist/test/loop/loopDebate.test.js dist/test/extensionHost/extensionHostExtractionContracts.test.js` | 当前扩展实例仍拥有 Loop 编排或相关 CLI 运行时，主任务 Tab 继续禁止关闭和重置；只要主任务记录为 `running`，主任务 Tab 即使没有直接 CLI 进程也显示“停止”并可中止，停止后父子任务统一落盘为 `stopped`；持久化为 `running` 但已无运行所有权的残留记录会收敛为 `stopped` 并解除锁定。重置会话只在扩展端完成“新建空白 Tab + 关闭旧 Tab”后刷新前端，拒绝操作不会伪装成已清空 |
| AI 对话 / Loop / 续跑 | 主任务 Tab 中断后输入续同一任务 | active | 终端用户 | `.ch/docs/references/cli-runtime-reference.md`、`.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/sessionMessageActions.ts`、`src/extension.ts`、`src/panelDiagnostics.ts` | `npm run build`；`node --test dist/test/session/sessionMessageActionsCoreCoverage.test.js dist/test/core/panelDiagnostics.test.js` | Loop 主任务 Tab 关联的任务处于 `needs-review`、`error`、`stopped` 或可恢复 `running` 状态时，在该 Tab 下继续发送任意 Loop 输入都会作为本次继续说明复用原 `taskId`，语义等同群聊“继续执行”；只有找不到可恢复任务时才允许新建 Loop 任务。Loop 子任务 Tab 手动输入仍强制走 coding 续跑，不启动新的 Loop 根任务 |
| AI 对话 / Loop | Loop 最大轮次全局设置与续跑提升 | active | 终端用户 | `.ch/docs/references/cli-runtime-reference.md`、`.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/extension.ts`、`src/loopTaskStore.ts` | `npm run build`；`node --test dist/test/loop/loopTaskStoreCoreCoverage.test.js` | `loopMaxRounds` 保存在全局工具设置，默认 20、范围 1-100。新建任务写入当前值；已有任务在恢复、继续或下一轮运行前若记录上限低于当前全局设置，会提升任务记录的 `maxRounds`，因此可把旧 20 轮任务扩展到新的上限；降低全局设置不会回写降低已有任务，避免运行中任务被意外截断 |
| AI 对话 / Loop+ | Loop+ 单次派发子任务上限 | active | 终端用户、Loop+ 主智能体 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/design-docs/loop-plus-scheduling.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/loopPlusDecision.ts`、`src/toolSettings.ts`、`src/extensionHost/loopPlusOrchestration.ts`、`src/extensionHost/loopPlusPromptBuilders.ts`、`src/webview/viewContentHtml.ts` | `npm run build`；`node --test dist/test/loop/loopPlusDecision.test.js dist/test/extensionHost/loopPlusPromptBuilders.test.js dist/test/extensionHost/loopPlusOrchestration.test.js dist/test/core/toolSettings.test.js dist/test/session/sessionMessageActionsCoreCoverage.test.js dist/test/webview/cliPageStaticRenderCoverage.test.js dist/test/webview/clipagescriptruntimecoverage.test.js` | 工具设置“AI任务配置”提供 `loopPlusDecisionSubtaskMax`，写入 `~/.sinitek_cli/settings.json`。默认 6，范围 1–20。该值限制 Loop+ 一次 `dispatch` 或 `accept` 可附带的子任务数，并写入下一轮主任务协议提示；未配置时仍是 6。它不改变经典 Loop 的 `LOOP_PARALLEL_SUBTASK_MAX`，也不改变 Loop+ 同时运行上限 `maxConcurrency`。超出并发上限的已接受子任务仍进入 pending。 |
| AI 对话 / Loop | 子任务手动恢复完成收尾一致性 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/loopSubtaskLifecycle.ts`、`src/extension.ts` | `npm run build`；`node --test dist/test/loop/loopSubtaskLifecycle.test.js dist/test/session/sessionMessageActions.test.js` | 子任务手动中断后在子任务 Tab 继续，若成功结束则与自动重试成功复用同一状态落盘和自动关标签流程；再次错误或停止时不自动关标签，主任务仅在可恢复且未触发连续失败上限时唤醒 |
| AI 对话 / Loop | 子任务中断不终止主任务 | active | 终端用户、Loop 主智能体、Loop 子智能体 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/extension.ts`、`src/extensionHost/loopOrchestration.ts`、`src/extensionHost/promptRunRuntime.ts`、`src/loopSubtaskLifecycle.ts`、`src/loopDebate.ts` | `npm run build`；`node --test dist/test/loop/loopSubtaskLifecycle.test.js dist/test/loop/loopDebate.test.js dist/test/extensionHost/extensionHostExtractionContracts.test.js dist/test/session/sessionMessageActionsCoreCoverage.test.js` | 单个子任务停止或重试耗尽失败只将该子任务保留为待续跑状态，父任务继续保持 `running`，且 `activeSubtaskIds` 保留该子任务；手动续跑成功后只有全部活动子任务均完成才唤醒主任务复核。串行批次中尚未派发的后续子任务会标为 `skipped` 并交由下一轮重新评估。用户从 Loop 群聊停止整个任务时，父子任务仍统一标为 `stopped` |
| AI 对话 / Loop | 子任务批次错峰启动 | active | 终端用户、Loop 主智能体、Loop 子智能体 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/design-docs/loop-debate-multi-agent-mode.md` | `src/extensionHost/loopOrchestration.ts` | `npm run build`；`node --test dist/test/loop/loopSubtaskLifecycle.test.js` | 多个 Loop 子任务按原有冲突组规划执行，但同一批次的子任务按 3 秒间隔逐个启动；已经启动且互不冲突的子任务仍可并行运行，任务中断时尚未启动的子任务标记为 `skipped` |
| AI 对话 / Loop | 开发级子任务高级 Skill 指导 | removed | Loop 主智能体、子智能体 | `.ch/docs/exec-plans/completed/2026-07/2026-07-12-loop-subtask-skill-injection.md` | 已删除 `media/loop-workflow-skills/`、`src/loopSkillGuidance.ts` 及同步/校验脚本 | 已由本次移除回归覆盖 | 2026-07-15 下线；不再加载或注入内置 Workflow Skills，也不提供开关 |
| AI 对话 / Loop | 子任务项目规则隔离与最少轮次执行 | active | Loop 主智能体、子智能体 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/loopSubtaskExecutionRoot.ts`、`src/shared/fsCleanup.ts`、`src/extension.ts`、`src/cli/commandRunner.ts`、`src/interactive/claudeRunner.ts` | `npm run build`；`node --test dist/test/loop/loopSubtaskExecutionRoot.test.js dist/test/shared/fsCleanup.test.js dist/test/cli/opencodeCommandRunner.test.js dist/test/loop/loopPromptBuilders.test.js dist/test/loop/loopPromptQueue.test.js dist/test/loop/loopMainFailure.test.js dist/test/loop/loopSubtaskThinking.test.js` | 主任务保留真实工作区和项目规则；子任务在隐藏根规则与项目 Skills 目录的临时根中执行，并使用各 CLI 的隔离参数。Windows 上目录用 junction、顶层文件优先 NTFS hardlink，跨盘时把临时根放到工作区 `.sinitek-loop-tmp`，删除失败只记清理结果不把成功任务打成失败。子任务遵循主任务下达的自包含范围，在单一连续执行回合内完成实现，只运行直接证明本次改动的最小必要检查 |
| AI 对话 / Loop | 子任务疑问静默转交 | active | Loop 主智能体、子智能体 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/extension.ts`、`src/loopTaskStore.ts` | `npm run build`；`node --test dist/test/loop/loopPromptBuilders.test.js dist/test/loop/loopPromptQueue.test.js` | 子任务遇到需求不明、授权不足、依赖/写入冲突或必须确认的问题时立即停止实施，把问题、事实、影响、选项与建议写入沟通文件的“待主任务确认”章节，按 completed 正常结束并只输出固定中性收口文本；不得在用户可见 assistant 回复中提问或复述问题。主任务读取后自行决策、继续派发，确需用户或人工确认时走 blocked 路径 |
| AI 对话 / Graph | Graph 可视运行图、恢复与最小控制 | active | 终端用户 | `.ch/docs/design-docs/graph-orchestration-mode.md`、`.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/exec-plans/completed/2026-07/2026-07-23-graph-phase2-recovery-visualization.md`、`.ch/docs/exec-plans/completed/2026-07/2026-07-24-graph-ai-planned-dag.md` | `src/graph/`、`src/extension.ts`、`src/promptRunState.ts`、`src/sessionMessageActions.ts`、`src/sessionMessageHandlers.ts`、`src/panelDiagnostics.ts`、`src/panelStateBuilder.ts`、`src/webview/graphRunPanel*.ts`、主 Webview 模式选择和消息 action | `npm run build`；`node --test dist/test/graph/graphRunPanel.test.js` 19/19；`git diff --check` | Webview 可选择 Graph，后端先创建 planning-only Graph run，由 `plan` AI planner 在 `## JSON` 输出 `plannedGraph.nodes` / `plannedGraph.edges` 后 materialize 为真实 DAG；支持分支、fan-out/fan-in、测试、评审、sleep、merge、summary、项目工作区 direct 执行；新 planner 拒绝 `human_gate` / `human_approved` / `manual`、Graph tab `🗺️`、主 tab 图级运行态、独立节点 conversation tab、可视 DAG 面板、主 Graph tab 唯一普通打开入口、指定/最近 run 打开、Continue/Resume 时选择保持原主/子模型或当前 Loop 主/子模型配置、“我要说话”、Retry、direct 自动返工、Stop、sleeping auto wake；节点返回 blocked 会归一为 failed 并进入 retry / `if_fail` / failed 复核路径，不再弹出 blocked modal 或跳过下游 quick pick；历史 worktree run 仍保留 metadata 读取、checkpoint/merge-back/cleanup 兼容处理，新 Graph run 不再创建 worktree/checkpoint/merge-back。主 Graph tab 的视觉运行态跟随图级生命周期，`sleeping` 等等待态仍显示运行中；failed / 历史 blocked 节点导致的 `needs-review` 会让主 tab 进入错误态并释放运行态；主 Graph tab 右下角 AI 对话“中止”和 GraphRunPanel Stop 共用同一 Graph stop 控制链，会把 run / active node 状态落盘为 `stopped`，并防止异步 tick 用旧状态覆盖 stopped。可视 DAG 使用对齐目标工作流画布的 `@dagrejs/dagre` full-canvas left-to-right layered layout，支持按当前节点尺寸调优的层间距/节点间距/边距、布局后碰撞消解、长标题/多下游节点动态高度、dagre 失败时拓扑层级兜底、12-port 自动连线、每个方向只渲染一条代表边、正反向边使用不同连接点、仅渲染流程边、隐藏 `evidence_for` / `conflicts_with` 等非流程连线、依赖边不显示可见文字、非依赖流程边保持正向单行短标签（完整说明保留在 title/aria/list）、已经过边按类型高亮：`depends_on` 保持 VS Code 主题蓝色，`if_pass` 绿色、`if_fail` 红色、`review_feedback` 黄色、`human_approved` 橙色，未经过边保持原样、Start/Decision/End/Step 语义 chip、按 `node.kind` 使用 VS Code 主题变量着色的矩形类型卡片、用户拖拽节点微调当前 run 的可视位置、背景左键按住拖拽平移，节点拖拽按 zoom 比例换算，边 path/端口属性/标签坐标/画布同步重算，拖拽移动不误触发详情，节点位置和 zoom 按 `graphRunId` 本地持久化；右上角缩放下拉固定 25%、50%、75%、100%、125%，默认 75%，定位运行节点按钮会将当前 `running` 节点自动滚动到画布可见区域中间，Reset 仅为紧凑控件，不再作为占空间长文案且不重置 zoom。新增单击节点详情弹窗，详情弹窗承载节点详情、控制入口和 Evidence/证据区；新运行时不再生成 human gate 审批 CTA。新增证据区展示选中节点 artifact、communication file、验收 evidenceRef、事件和 finalAnswer evidence 引用。review 节点 prompt 会从上游节点 `writeFiles`、communication file 和 artifactRef 生成评审范围，要求按候选改动文件过滤 git status/diff，范围外 dirty workspace 改动不单独导致 failed。Graph completed 后主 Graph tab 会追加 `graphFinalSummary=true` 的 assistant 最终总结气泡，内容来自主模型执行的 `summary` 节点 `finalAnswer`。Graph UI 不再常驻展示 Stop 能力边界说明；具体 Stop 操作结果或错误仍通过运行消息/状态反馈表达。限制：无图编辑器/模板库/运行前确认/DAG 结构编辑/边节点编辑/完整人工审批工作流/复杂布尔条件编辑器/任意条件重写/可视化重规划/rollback 预演/证据文件正文读取；背景平移、12-port、非依赖流程边短标签、语义 chip 和类型着色矩形卡片仅为视觉层优化；direct 模式无 checkpoint/merge-back/git rollback，Retry 只在当前工作区状态上重跑节点；direct run 若失败分类推荐 `direct_rework` 且存在 active `review_feedback` / `if_fail` 显式返工边，会自动重置声明返工范围并继续调度；Feedback rollback 只对历史 worktree/baseCommit 可用的旧 run 提供；Stop 只保证 Graph 状态和事件落盘，真实 CLI 停止仅对已映射 active run 发起请求且不确认退出 |
| AI 对话 / Graph | Graph 动态扩图续跑 | active | 终端用户、Graph planner | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/exec-plans/completed/2026-08/2026-08-11-graph-dynamic-replanning.md`、`.ch/docs/exec-plans/completed/2026-08/2026-08-29-graph-parallel-scheduler.md` | `src/graph/graphPlanner.ts`、`src/graph/graphPromptBuilders.ts`、`src/extensionHost/graphRuntime.ts` | `npm run build`；`node --test dist/test/graph/graphPlanner.test.js dist/test/graph/graphPromptBuilders.test.js dist/test/graph/graphExtensionRuntime.test.js`；`npm run validate:whitespace -- src/graph/graphPlanner.ts src/graph/graphPromptBuilders.ts src/extensionHost/graphRuntime.ts src/test/graph/graphPlanner.test.ts src/test/graph/graphPromptBuilders.test.ts src/test/graph/graphExtensionRuntime.test.ts .ch/docs/product-specs/sinitek-cli-plugin-capabilities.md .ch/docs/product-specs/FEATURE_INVENTORY.md docs/插件功能清单.md .ch/docs/exec-plans/completed/2026-08/2026-08-11-graph-dynamic-replanning.md`；`git diff --check` | Graph run 在直接返工不可用、失败/阻塞节点需要新增返工范围，或无可运行节点卡住时，会在当前 run 内追加最多 3 个主模型 `replan-*` 节点继续规划；普通成功推进 tick 不追加 `replan-*`，而不是新建 Graph run。`replan-*` 通过 `plannedGraph` 增量追加新增节点/边，append materialize 拒绝覆盖已有节点或让新增边指向已有节点；旧 failed/blocked 节点通过 `if_fail` 或 `evidence_for` 作为触发/证据，新增根节点自动依赖当前 replanner，并由新的续跑 summary 收束。 |
| AI 对话 / Graph | AI planner 并行优先 DAG 规划 | active | 终端用户、Graph planner | `.ch/docs/design-docs/graph-orchestration-mode.md`、`.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/exec-plans/completed/2026-08/2026-08-02-graph-planner-parallelism.md`、`.ch/docs/exec-plans/completed/2026-08/2026-08-29-graph-parallel-scheduler.md` | `src/graph/graphPlanner.ts`、`src/graph/graphPromptBuilders.ts`、`src/graph/graphScheduler.ts`、`src/extensionHost/graphRuntime.ts` | `npm run build`；`node --test dist/test/graph/graphScheduler.test.js dist/test/graph/graphExtensionRuntime.test.js dist/test/graph/graphPlanner.test.js dist/test/graph/graphKernel.test.js dist/test/graph/graphRunControl.test.js` 48/48；`node --test dist/test/graph/graphPromptBuilders.test.js dist/test/graph/graphFailureClassification.test.js dist/test/graph/graphNodeLifecycle.test.js dist/test/graph/graphStore.test.js dist/test/graph/graphNodeArtifact.test.js` 37/37；`npm run validate:whitespace -- src/graph/graphScheduler.ts src/extensionHost/graphRuntime.ts src/test/graph/graphScheduler.test.ts src/test/graph/graphExtensionRuntime.test.ts src/test/graph/graphPlanner.test.ts .ch/docs/design-docs/graph-orchestration-mode.md .ch/docs/product-specs/sinitek-cli-plugin-capabilities.md .ch/docs/product-specs/FEATURE_INVENTORY.md docs/插件功能清单.md .ch/docs/runbooks/PITFALLS.md .ch/docs/exec-plans/completed/2026-08/2026-08-29-graph-parallel-scheduler.md`；`git diff --check` | AI planner prompt 默认要求先寻找可并行分支，把多个可拆分且互不冲突的任务设计成 planner fan-out、test/review/merge/summary fan-in 的 DAG；不得仅因列表顺序或共享最终验收串行化独立分支。planner 漏填 `plannedGraph.maxConcurrent` 时，materialize 会按首批仅依赖 `plan` 且通过 scheduler 冲突判定的可执行根节点推断并发上限；运行时只因同一 `conflictGroup` 或重叠 `writeFiles` 串行化，未声明 `writeFiles` / `conflictGroup` 的 ready 节点可同批并行但必须被规划约束视为不写文件，上限不超过既有 5 个。 |
| AI 对话 / Graph | Advisory 验证节点不中断收束 | active | Graph planner、终端用户 | `.ch/docs/design-docs/graph-orchestration-mode.md`、`.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/exec-plans/active/2026-08-02-graph-advisory-validation.md` | `src/graph/types.ts`、`src/graph/graphPlanner.ts`、`src/graph/graphStore.ts`、`src/graph/graphScheduler.ts`、`src/graph/graphPromptBuilders.ts`、`src/graph/graphFailureClassification.ts` | `npm run build`；`node --test dist/test/graph/graphScheduler.test.js dist/test/graph/graphPlanner.test.js dist/test/graph/graphStore.test.js dist/test/graph/graphFailureClassification.test.js dist/test/graph/graphPromptBuilders.test.js` | 完整单测、全仓测试、全量 lint 等覆盖面大且可能包含历史/范围外失败的验证节点可声明 `blocking:false`，失败后仍保留 failed 状态并进入 review/summary 的 evidence 与 unresolved，但不会单独阻断结构依赖继续执行；失败分类会把 `dist/test/*.test.js` 候选映射回 `src/test/*.test.ts`，便于后续返工判断真实源码测试范围。 |
| AI 对话 / Graph | 条件边与验证失败反馈返工 | active | 终端用户 | `.ch/docs/design-docs/graph-orchestration-mode.md`、`.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/exec-plans/completed/2026-07/2026-07-24-graph-validation-feedback-rollback.md` | `src/graph/types.ts`、`src/graph/graphPlanner.ts`、`src/graph/graphStore.ts`、`src/graph/graphScheduler.ts`、`src/graph/graphRunControl.ts`、`src/extensionHost/graphRuntime.ts`、`src/graph/graphPromptBuilders.ts`、`src/webview/graphRunPanel*.ts` | `npm run build`；`npm run validate:whitespace -- src/graph/types.ts src/graph/graphFailureClassification.ts src/graph/graphRunControl.ts src/extensionHost/graphRuntime.ts src/test/graph/graphRunControl.test.ts src/test/graph/graphFailureClassification.test.ts src/test/graph/graphExtensionRuntime.test.ts src/test/shared/trailingWhitespaceCheck.test.ts scripts/check_trailing_whitespace.js src/extensionHost/promptOneShotRuntime.ts`；`node --test dist/test/graph/graphRunControl.test.js dist/test/graph/graphFailureClassification.test.js dist/test/graph/graphExtensionRuntime.test.js dist/test/shared/trailingWhitespaceCheck.test.js` 29/29；`node --test dist/test/graph/graphScheduler.test.js dist/test/graph/graphNodeLifecycle.test.js dist/test/graph/graphStore.test.js dist/test/graph/graphNodeArtifact.test.js` 34/34 | Edge 记录和 planner edge spec 保留 `label`、`conditionExpression`、`metadata`；scheduler 支持 `source_status`、`source_acceptance` 条件求值；`manual` 仅历史兼容，新 planner 会拒绝生成；custom/不可求值条件保守进入失败/复核口径且输出可读 blocker。历史 worktree/baseCommit 可用的 failed / 历史 blocked 验证类节点可 Feedback rollback；宿主优先按 active `review_feedback` / `if_fail` edge 或上游依赖选择返工目标，记录 target selection、候选、reset scope、feedback reason 和触发 edge，被重置节点写入 `rework`，后续 prompt/summary 可见返工来源和范围；direct run 不提供 Feedback rollback 按钮或 checkpoint rollback，但若失败分类推荐 `direct_rework` 且显式反馈边可用，会自动重置声明 scope、返工目标和失败源节点并继续调度。仍不提供边编辑器、局部返工路径编辑、自动修复分支、任意条件重写/可视化重规划或可视 rollback 预演 |
| AI 对话 / Graph | Graph 失败分类与 needs-review 返工建议 | active | 终端用户、Graph planner | `.ch/docs/design-docs/graph-orchestration-mode.md`、`.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/runbooks/PITFALLS.md` | `src/graph/types.ts`、`src/graph/graphFailureClassification.ts`、`src/graph/graphNodeLifecycle.ts`、`src/graph/graphStore.ts`、`src/graph/graphNodeArtifact.ts`、`src/extensionHost/graphRuntime.ts`、`src/extension.ts`、`scripts/check_trailing_whitespace.js` | `npm run build`；`npm run validate:whitespace -- src/graph/types.ts src/graph/graphFailureClassification.ts src/graph/graphRunControl.ts src/extensionHost/graphRuntime.ts src/test/graph/graphRunControl.test.ts src/test/graph/graphFailureClassification.test.ts src/test/graph/graphExtensionRuntime.test.ts src/test/shared/trailingWhitespaceCheck.test.ts scripts/check_trailing_whitespace.js src/extensionHost/promptOneShotRuntime.ts`；`node --test dist/test/graph/graphRunControl.test.js dist/test/graph/graphFailureClassification.test.js dist/test/graph/graphExtensionRuntime.test.js dist/test/shared/trailingWhitespaceCheck.test.js` 29/29 | 失败节点会落盘 `GraphNodeRecord.failure`，`node.failed` event data 写入 `failureClassification`；needs-review / idle 文案展示 category、confidence、signals、recommendedRecovery、recommendedWriteFiles 和 nodeDraft。重构/迁移导致旧测试契约失效且验证节点缺少测试 `writeFiles` 时，分类为 `missing_write_scope` 并保留 `stale_test_contract` signal，推荐新增测试契约适配节点而不是单纯 Retry；direct run 中带显式 `review_feedback` / `if_fail` 返工边的实现缺陷推荐 `direct_rework`，避免继续输出不可执行的 direct checkpoint rollback 建议；`validate:whitespace` 覆盖 untracked 新文件行尾空白，避免评审节点重复失败 |
| AI 对话 / Graph | GraphRunPanel 目标系统式自动布局升级 | active | 终端用户 | `.ch/docs/design-docs/graph-orchestration-mode.md`、`.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/webview/graphRunPanel.ts`、`src/test/graph/graphRunPanel.test.ts` | `npm run build`；`node --test dist/test/graph/graphRunPanel.test.js` 19/19 | GraphRunPanel 默认 LR，但内部支持 `LR` / `RL` / `TB` / `BT` 方向；使用目标系统 workflow 画布的 dagre + fallback / collision / 端口 / 视口经验，包含调优间距、多根零入度 fallback、`review_feedback` 与上游 `if_fail` non-ranking 回边、方向感知 collision、端口分离，以及 selected/running/sleeping/blocked/failed 初始居中。该升级仅优化当前 run 可视表达，不改变 DAG 结构或调度语义 |
| AI 对话 / 记忆 | Harness 骨架开关与踩坑记录 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`docs/LONG_TERM_MEMORY_DESIGN.md` | 工具设置、prompt 构建、当前工作区 `.ch/.agents` harness scaffold、`AGENTS.md`、`CLAUDE.md`、`.gitignore` 的 `.codegraph/` 忽略项、`ARCHITECTURE.md` AI 初始化任务、`.ch/docs/memory/`、`.ch/docs/runbooks/PITFALLS.md`、CodeGraph 终端初始化与工作区 CodeGraph 开关 | `npm run build`；`node --test dist/test/core/toolSettings.test.js dist/test/memory/memoryRuntimeGate.test.js dist/test/webview/cliPageStaticRenderCoverage.test.js dist/test/webview/clipagescriptruntimecoverage.test.js dist/test/session/sessionMessageHandlersCoreCoverage.test.js dist/test/cli/codegraphStatus.test.js dist/test/extensionHost/workspaceHarnessInitialization.test.js` | 默认关闭；当前工作区已存在 `.ch` 目录时视为已安装，开关展示为已勾选且禁用；开启时先确认，确认后补齐工作区 harness scaffold、确保 `.codegraph/` 被 git 忽略，不自动安装 CodeGraph、注册 MCP 或初始化索引；工作区页独立 CodeGraph 开关与 harness 骨架相同，用复选框点击开始安装；当前工作区存在 `.codegraph/` 索引目录时直接显示已勾选且禁用，没有索引时点击才开始安装；尚未安装时，对话或 Loop 主任务运行态不会禁用这两个复选框；`src/cli/codegraphStatus.ts` 的完整安装监测仍要求本机 CLI、MCP 交互和当前工作区索引全部 ready，Windows 使用 `cmd.exe`、macOS 使用默认 shell；收尾阶段可二次确认并复用当前 AI 对话以 coding 模式初始化 `ARCHITECTURE.md`；`PITFALLS.md` 记录带根因/规避/验证线索的失败、阻塞、回滚或明确踩坑总结；关闭后只允许查看/导出/删除，不控制 Codex / Claude / OpenCode 外部 CLI 自带记忆或历史 |
| AI 对话 / Codex / OpenCode | 全局隐式子代理开关与子代理进度气泡 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md`、`.ch/docs/exec-plans/completed/2026-07/2026-07-13-unified-codex-opencode-multi-agent-toggle.md` | 全局工具设置、`src/toolSettings.ts`、`src/workspaceSettingsStore.ts`、`src/sessionMessageActions.ts`、`src/interactive/codexRunner.ts`、`src/interactive/codexRunnerRuntime.ts`、`src/cli/opencodeconfigmodels.ts`、`src/cli/opencoderuntimeconfig.ts`、`src/cli/commandRunner.ts`、`src/openCodeTabStream.ts`、`src/extension.ts`、`src/subagentProgress.ts`、Webview 消息路由 | `npm run build`；`node --test dist/test/core/toolSettings.test.js dist/test/interactive/codexRunnerRuntime.test.js dist/test/cli/opencodeconfigmodels.test.js dist/test/cli/opencoderuntimeconfig.test.js dist/test/core/workspaceSettingsStore.test.js dist/test/session/sessionMessageActions.test.js dist/test/webview/multiAgentSettingWebview.test.js dist/test/cli/commandRunnerCoverage.test.js dist/test/core/openCodeTabStream.test.js` | 工具设置“AI任务配置”Tab 展示“隐式子代理”，`multiAgentEnabled` 保存在 `~/.sinitek_cli/settings.json`，默认关闭；旧工作区 `multiAgentEnabled` / `codexMultiAgentEnabled` 仅作为迁移输入，全局值优先。关闭时 Codex app-server 显式禁用 `multi_agent`；OpenCode 本次 `OPENCODE_CONFIG` 临时 overlay 合并顶层 `permission.task="deny"`，并以更高优先级的 `OPENCODE_CONFIG_CONTENT` 内联覆盖重申该拒绝，因此项目配置不能重新放开子代理；两者都不写回用户配置。开启时撤销插件侧禁用并保留 CLI 自身可用的隐式子代理能力与 OpenCode task 权限。OpenCode `step_start` 仅作内部进度 bookkeeping，不再生成“OpenCode is planning the next step…”思考气泡；独占 `...` / `…` 占位文本也不会创建 assistant 气泡；真实 reasoning、工具调用和 task 列表仍正常展示。该开关不控制 Loop 的 `main_sub_multi_agent` / `debate_multi_agent` 编排。Codex 子代理仍按 App Server `threadId` 分流独立 assistant 气泡，并发输出不会合并，子线程不会覆盖/结束父线程，子代理文本不参与父任务 final-answer 判定 |
| AI 对话 / Codex | 新建会话悬停查看长连接数 | active | 终端用户 | `.ch/docs/design-docs/vscode-cli-extension-runtime.md` | `src/interactive/codexAppServerPool.ts`、`src/sessionMessageHandlers.ts`、`src/webview/viewContentHtml.ts`、`src/webview/viewContentScript/eventBindings.ts`、`src/webview/viewContentScript/windowMessageDispatch.ts` | `npm run build`；`node --test dist/test/interactive/codexAppServerPool.test.js dist/test/webview/codexLongConnectionTooltip.test.js dist/test/session/sessionMessageHandlersCoreCoverage.test.js dist/test/webview/cliPageStaticRenderCoverage.test.js` | 鼠标在 AI 对话面板“新建会话”图标上停留 3 秒后，显示当前仍存活的 Codex app-server 长连接数。短停留仍只显示“新建会话”。离开按钮后提示消失，过期响应不会显示。计数只统计进程仍存活的连接，不按会话或 thread 重复计算。 |
| AI 对话 / Codex | 跨模型/provider thread 续接 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md`、`.ch/docs/design-docs/vscode-cli-extension-runtime.md`、`.ch/docs/exec-plans/completed/2026-08/2026-08-25-codex-app-server-provider-resume.md` | `src/interactive/codexThreadSelection.ts`、`src/interactive/codexRuntimeConfig.ts`、`src/interactive/codexRunnerRuntime.ts`、`src/interactive/codexRunner.ts`、`src/extensionHost/promptInteractiveRuntime.ts`、`src/test/interactive/codexThreadSelection.test.ts`、`src/test/interactive/codexRunnerRuntime.test.ts`、`src/test/interactive/codexRuntimeConfig.test.ts`、`src/test/interactive/codexRunnerLifecycle.test.ts` | `npm run build`；`node --test dist/test/interactive/codexThreadSelection.test.js dist/test/interactive/codexRunnerRuntime.test.js dist/test/interactive/codexRuntimeConfig.test.js dist/test/interactive/codexRunnerLifecycle.test.js` | Codex 交互会话在插件侧模型或配置档案变化后继续复用已映射 `threadId`，不再由插件主动冻结旧 thread 并新建会话。相同启动参数共用一条 app-server 连接，只 `initialize` 一次；当前进程已加载的 thread 后续只发 `turn/start`。runner 从当前 `~/.codex/config.toml` 读取根级 `model_provider`，只在冷 `thread/start` / `thread/resume` 传 `modelProvider`，`turn/start` 继续传 `model`、effort、cwd、sandbox 和 approval。provider 变化会换连接后再冷恢复。Loop 子任务重试复用原 tab / thread。恢复使用服务端 thread，不重新执行旧工具调用或重发完整历史，废弃的 `persistExtendedHistory` 不作为恢复开关；跨 provider/account 的加密 reasoning/compaction 内容若不兼容，app-server 原始错误会保留并收口。 |
| AI 对话 / Codex | 特殊符号提示词可发起会话 | active | 终端用户 | `.ch/docs/design-docs/vscode-cli-extension-runtime.md`、`.ch/docs/references/cli-runtime-reference.md`、`.ch/docs/runbooks/PITFALLS.md` | `src/interactive/codexAppServerNdjson.ts`、`src/interactive/codexRunner.ts`、`src/test/interactive/codexAppServerNdjson.test.ts` | `npm run build`；`node --test dist/test/interactive/codexAppServerNdjson.test.js dist/test/interactive/codexRunnerLifecycle.test.js` | 提示词或 app-server 事件中的 U+2028/U+2029 行分隔符、引号、反斜杠和普通换行不再让会话启动失败。插件按 LF/CRLF 读取 NDJSON，并在写入 JSON-RPC 时转义行分隔符，避免出现 `Unterminated string in JSON`。 |
| AI 对话 / 通用 | 最终答复固定协议与结构化收口 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/finalAnswerProtocol.ts`、`src/finalConclusion.ts`、共享 prompt 构建、Codex app-server 主 turn completed 回调、OpenCode JSONL `stop` 终态关联、最终结论气泡判定 | `npm run build`；`node --test dist/test/webview/finalAnswerPolicy.test.js dist/test/extensionHost/promptRuntime.test.js dist/test/core/finalConclusion.test.js dist/test/extensionHost/promptInteractiveRuntime.test.js dist/test/interactive/codexRunnerRuntime.test.js dist/test/interactive/codexRunnerSubagent.test.js dist/test/interactive/codexRunnerLifecycle.test.js dist/test/cli/opencodeCommandRunner.test.js dist/test/core/toolSettings.test.js dist/test/session/sessionMessageActions.test.js` | 普通任务 prompt 和 hidden retry prompt 固定要求模型最终回复包含 `[final_answer]`，没有用户可切换策略。运行时按结构化/协议信号收口：Codex 接受 `phase="final_answer"` / `codexFinalAnswer=true`；OpenCode 接受同一 `messageID` 的非 thinking assistant `text` + `step_finish.reason="stop"`；Codex / Claude / OpenCode 仍接受当前用户消息之后包含 `[final_answer]` 的非 thinking assistant 文本。普通任务最终回复气泡只认消息类型 `final_answer`（`codexFinalAnswer=true`）或非 thinking 正文包含 `[final_answer]`；Codex 主 turn 的 `turn.completed status="completed"` 仅表示回合结束，不能单独收口；若最后一条主线程 `agent_message` 为 `phase:null` / 缺省 phase 且其后没有工具活动，则提升为 `codexFinalAnswer=true`。reasoning/thinking-only 或 `phase:"commentary"` 后的 completed turn 进入 hidden retry。Loop 等内部机器协议继续使用自身结构化终态，不注入或要求文本标记；遗留 `finalAnswerPolicy` / `codexFinalAnswerPolicy` 设置被忽略 |
| AI 对话 / 通用 | 最终答复协议标记展示过滤 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | Webview assistant 气泡渲染 | `npm run build`；`node --test dist/test/webview/finalAnswerPolicy.test.js` | 仅从 assistant 气泡展示文本移除开头的 `[final_answer]` 协议前缀，行内标记保持可见；不修改用于严格判定、会话续接和存档的原始消息，user 气泡保持原样 |
| AI 对话 / Codex | 推理摘要清洗与实时思考气泡 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/codexReasoningContent.ts`、`src/interactive/codexRunner.ts`、`src/interactive/codexRunnerRuntime.ts`、Codex thinking 会话清洗 | `npm run build`；`node --test dist/test/core/codexReasoningContent.test.js dist/test/interactive/codexAppServerProtocol.test.js dist/test/interactive/codexRunnerRuntime.test.js dist/test/interactive/codexRunnerLifecycle.test.js` | 仅从 Codex reasoning/thinking 内容移除独占一行的空 HTML 注释 `<!-- -->`（含水平空白变体），并截掉思考正文中泄漏的 `[final_answer]` 草稿；普通 assistant/user 内容、行内空注释和非空 HTML 注释保持原样。交互运行会消费 `item/reasoning/summaryTextDelta` / `textDelta` / `summaryPartAdded` 实时写入 thinking 气泡，同一 reasoning item 的 delta 在工具 trace 插入后仍写入原思考气泡，完成快照不得用更短摘要覆盖已流式思考。所有模型的 thread/start 与 thread/resume 都设置 `model_reasoning_summary=detailed`，不再按模型名包含 `grok` 分支。插件不按 200 字裁切思考正文；GPT 日志摘要是完整短标题，Grok/kedaya 上游仍可能在约 200 字处结束并带上 `...`，`detailed` 不能补回未下发文本。加载历史 Codex 会话时同步清理已落盘残片 |
| AI 对话 / 通用 | 内部思考 wrapper 定向清洗 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/thinkingMarkup.ts`、OpenCode JSONL text/reasoning 解析、Codex reasoning 与历史消息清洗 | `npm run build`；`node --test dist/test/cli/opencodeCommandRunner.test.js dist/test/core/codexReasoningContent.test.js` | 仅识别 `<thinking>`、`<think>`、`<analysis>`、`<reasoning>` 四类内部 wrapper；混合 text 事件拆成 thinking 与 assistant，最终正文排除思考块，历史消息去除 wrapper；普通 `<div>` 等 HTML/代码标签保持原样 |
| AI 对话 / OpenCode | `todowrite` 任务列表与工具标题本地化 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/cli/openCodeTaskList.ts`、`src/cli/commandRunner.ts`、`src/openCodeTabStream.ts`、`src/extension.ts`、`src/webview/viewContentHtml.ts`、`src/webview/viewContentStyles/tasklist.ts`、`src/webview/viewContentScript/taskListAndUi.ts`、`src/webview/viewContentScript/coreRuntimeState.ts`、`src/webview/viewContentScript/traceRendering.ts`、`src/webview/viewContentI18n.ts` | `npm run build`；`node --test dist/test/cli/openCodeTaskList.test.js dist/test/core/openCodeTabStream.test.js dist/test/webview/openCodeTaskListOverlay.test.js dist/test/cli/opencodeCommandRunner.test.js dist/test/webview/clipagescriptruntimecoverage.test.js dist/test/webview/traceToolTitleLocalization.test.js` | OpenCode 从 JSONL `tool_use/todowrite` 的 `state.input.todos`（兼容 metadata/output）实时更新当前或并行 conversation tab 的任务列表，空数组清空；每次新 AI run 启动会先向目标 tab 发送空 `taskListUpdate` 并清除重放缓存，避免旧列表跨任务残留；专用 `taskListUpdate` 与同一 tool trace 的任务元数据形成双通道，Webview 重载时会重放运行中列表，运行中的消息刷新不再清空浮层。任务列表标题有收起/展开箭头，收起后仅保留标题、进度和箭头，进度按已完成/总数显示，例如 `2/4`；展开后的任务项按单行高度最多显示约 5 条，少于 5 条时随内容收缩，更多时在列表内滚动；常见 tool-use 气泡标题按中英文界面本地化，未知工具与详情正文保持原样。Loop 与 Loop+ 主任务 Tab 不展示任务列表浮层，也不因父任务仍忙碌而保留旧 external 列表 |
| AI 对话 / Codex | assistant `Tasklist` 任务列表解析与气泡过滤 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/promptRuntime.ts`、`src/interactive/codexAppServerProtocol.ts`、`src/extension.ts`、`src/webview/viewContentScript/taskListAndUi.ts`、`src/webview/viewContentScript/messageRendering.ts`、`src/webview/viewContentScript/traceRendering.ts` | `npm run build`；`node --test dist/test/extensionHost/promptRuntime.test.js dist/test/interactive/codexAppServerProtocol.test.js dist/test/webview/clipagescriptruntimecoverage.test.js` | 普通 Codex 人类对话 prompt 会注入 TaskList logging requirement，要求进度更新统一使用 `Tasklist:` 标题和逐行 `- [pending]` / `- [in_progress]` / `- [completed]` 状态，任务描述默认中文表达且代码符号、命令、路径、包名和用户原文术语可保留原文；新 run 的 `runStatus:start` 会按目标 tab 先重置 taskList 起点并隐藏旧列表；Codex app-server assistant 消息仍从实际日志里的 `Tasklist:` / `Tasklist update:` / `Tasklist 更新：` 段落解析任务，兼容多行、checkbox、中文状态词和同行分号分隔；已解析的纯任务列表 assistant 气泡不再进入对话，普通说明混合 Tasklist 时仅剥离任务列表片段并保留正文。任务列表面板展开后按单行高度最多显示约 5 条，更多时在列表内滚动。Loop 与 Loop+ 主任务不展示该浮层，并在主任务提示中禁止复述旧 Tasklist；子任务和普通对话不受影响 |
| AI 对话 / 通用 | Vibe 人工交互表单 | active | 终端用户 | `.ch/docs/exec-plans/completed/2026-08/2026-08-08-human-interaction-vibe.md`、`.ch/docs/exec-plans/completed/2026-08/2026-08-08-human-interaction-natural-clarification.md`、`.ch/docs/design-docs/vscode-cli-extension-runtime.md` | 全局工具设置、`src/humanInteraction.ts`、`src/interactive/codexRunner.ts`、`src/interactive/codexRunnerRuntime.ts`、`src/extensionHost/promptInteractiveRuntime.ts`、`src/extensionHost/promptOneShotRuntime.ts`、`src/extensionHost/promptParallelRuntime.ts`、`src/promptRuntime.ts`、`src/extension.ts`、`src/webview/viewContentHtml.ts`、`src/webview/viewContentScript/settingsAndOverlays.ts`、`src/webview/viewContentScript/windowMessageDispatch.ts` | `npm run build`；`node --test dist/test/core/humanInteraction.test.js dist/test/extensionHost/promptRuntime.test.js dist/test/extensionHost/promptInteractiveRuntime.test.js dist/test/extensionHost/promptOneShotRuntime.test.js dist/test/extensionHost/extensionHostExtractionContracts.test.js dist/test/core/toolSettings.test.js dist/test/session/sessionMessageActions.test.js dist/test/webview/multiAgentSettingWebview.test.js dist/test/interactive/codexRunnerRuntime.test.js dist/test/interactive/codexRunnerLifecycle.test.js` | `humanInteractionEnabled` 保存到 `~/.sinitek_cli/settings.json`，默认开启，并在工具设置“AI任务配置”Tab 展示“人工交互”开关。开启后 Codex Vibe/coding 交互任务启动 app-server 时追加 `--enable default_mode_request_user_input`，并在 `initialize.capabilities.experimentalApi=true` 时声明 host 可处理 experimental request，使 Codex Default mode 的 `request_user_input` 能进入插件请求链；运行时可拦截 Codex app-server `item/tool/requestUserInput` 与 MCP `mcpServer/elicitation/request` 结构化请求。若用户原始 prompt 明确要求 AI 先询问需求/细节，而 Codex、Claude 或 OpenCode 只输出普通问题列表，运行时会移除该问题列表 assistant 气泡并兜底打开同一人工交互表单，且把问题文案中的“可选 / 选项 / 例如 / 如”候选项和紧随问题的 `A.` / `B.` / `C.` 字母选项列表解析为 radio/checkbox 字段；提交后把补充信息作为用户消息写入当前会话并继续同一 Codex thread、Claude session 或 OpenCode one-shot/parallel 会话；Loop/Graph 不复用该弹窗。Webview 使用当前 overlay/modal 主题变量渲染 text、password、textarea、radio、checkbox、select、multiselect 表单；结构化请求提交后把 answers/content 返回给 Codex 继续执行；拒绝或关闭表单会追加拒绝说明、终止当前任务并关闭弹窗，不进入 hidden retry |
| AI 对话 / Loop | 独立子任务主对话实时进度气泡 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/loopSubtaskProgress.ts`、`src/subagentProgress.ts`、`src/extension.ts`、Loop 主/子 conversation tab 消息存储与 Webview 定向消息协议 | `npm run build`；`node --test dist/test/loop/loopSubtaskProgress.test.js dist/test/core/subagentProgress.test.js dist/test/loop/loopSubtaskLifecycle.test.js dist/test/core/finalConclusion.test.js` | 每个 Loop 独立子任务启动后，主任务对话立即生成一个带子任务标题和执行中状态的 assistant 气泡；子任务 tab 的非 thinking、非内部子代理 assistant 快照每秒同步到该气泡，完成、失败或中断时原位收口。子任务自己的 assistant/thinking/trace 气泡继续按 tab 展示；主任务中的 Loop 进度气泡带稳定 `subagentId`，不参与父任务 final-answer 判定 |
| AI 对话 / OpenCode | 子代理事件同步与 60 秒补捞气泡 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/cli/openCodeSubagentMonitor.ts`、`src/cli/commandRunner.ts`、`src/subagentProgress.ts`、`src/extension.ts`、`src/i18n.ts`、one-shot / 并行 tab / Webview 消息路由 | `npm run build`；`node --test dist/test/cli/openCodeSubagentMonitor.test.js dist/test/core/subagentProgress.test.js dist/test/core/finalConclusion.test.js dist/test/core/openCodeTabStream.test.js dist/test/cli/opencodeCommandRunner.test.js` | OpenCode 父 `run --format json` 不转发内部子代理增量时，插件显式启动受管 `opencode serve`、等待健康后再以 `run --attach` 执行父任务；SSE 事件触发子会话消息快照刷新，并每 60 秒通过公开 children/message/status API 全量补捞。每个当前尝试新建的子 session 固定更新独立 assistant 气泡；任务结束统一关闭服务、订阅和轮询。服务不可用时只显示一次降级状态并继续父任务，SSE 重连使用最长 60 秒的指数退避；不读取私有 SQLite，子代理气泡不参与父任务 final-answer 判定 |
| AI 对话 / OpenCode | 当前工作区 cwd/PWD 一致执行 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/cli/commandRunner.ts`、`src/extension.ts` | `npm run build`；`node --test dist/test/cli/opencodeCommandRunner.test.js` | OpenCode one-shot、并行和 Loop 子任务以当前 VS Code 工作区为 spawn cwd，并把 child env `PWD` 强制同步为同一路径；即使 extension host 继承 `PWD=/`，OpenCode `run` 内部请求也不会再把新会话创建到文件系统根目录或要求用户重新选择项目；修复前已绑定 `/` 的历史会话需新建一次对话，不直接篡改 OpenCode 数据库 |
| AI 对话 / 会话 | 历史提示词收藏与仅收藏过滤 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/exec-plans/completed/2026-08/2026-08-09-prompt-history-favorites.md` | `src/promptHistoryStore.ts`、`src/extensionHost/modelSettings.ts`、`src/sessionMessageHandlers.ts`、`src/webview/viewContentHtml.ts`、`src/webview/viewContentScript/historyPanels.ts`、`src/webview/viewContentScript/runStreamAndQueue.ts`、`src/webview/viewContentScript/taskListAndUi.ts`、`src/webview/viewContentStyles/overlaysModals.ts` | `npm run build`；`node --test dist/test/session/promptHistoryStore.test.js dist/test/session/sessionMessageHandlersCoreCoverage.test.js dist/test/webview/clipagescriptruntimecoverage.test.js dist/test/loop/loopPromptQueue.test.js dist/test/session/sessionMessageActions.test.js` | 历史记录弹窗“历史提示词”页签每条提示词提供星标收藏按钮，收藏状态持久化到 prompt history 记录的 `favorite` 字段；旧记录缺失字段时按未收藏读取。页签顶部提供“仅收藏”过滤和收藏计数，过滤状态保存在 Webview 本地 UI 状态，不写入全局或工作区设置；过滤后无结果显示“暂无收藏提示词/No favorite prompts”，清空提示词仅清空未收藏项并保留所有收藏提示词；通过“加入列表”进入队列的提示词会在入队时立即写入历史提示词列表，队列后续自动或手动执行时携带 `skipPromptHistory`，不会重复写入；30 天 retention 和数量上限也只裁剪未收藏提示词 |
| AI 对话 / 会话 | 历史记录关键词搜索过滤 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`src/webview/viewContentHtml.ts`、`src/webview/viewContentScript/coreBootstrap.ts`、`src/webview/viewContentScript/historyPanels.ts`、`src/webview/viewContentScript/settingsAndOverlays.ts`、`src/webview/viewContentStyles/overlaysModals.ts`、`src/webview/viewContentI18n.ts` | `npm run build`；`node --test dist/test/webview/clipagescriptruntimecoverage.test.js dist/test/webview/cliPageStaticRenderCoverage.test.js dist/test/session/historySessionLoop.test.js dist/test/webview/runPromptHistoryWebview.test.js` | 历史记录弹窗提供共用关键词搜索框，实时过滤“历史提示词”和“历史会话”页签；提示词按正文、CLI 和记录 ID 匹配，会话按首条提示词、标签、CLI 和会话 ID 匹配，大小写不敏感；无匹配时显示对应中英文空状态；弹窗高度保持稳定，提示词和会话列表在各自面板内滚动，不因匹配数量或列表数量改变弹窗高度；不修改历史数据、收藏状态或会话清空行为。 |
| AI 对话 / 会话 | 当前会话提示词倒序查看 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | 对话运行状态区“提示词”按钮、当前会话消息运行态、提示词弹层 | `npm run build`；`node --test dist/test/webview/runPromptHistoryWebview.test.js` | 仅展示当前 tab 会话的非空用户输入；按 `createdAt` 倒序，缺少时间时按消息顺序倒序；最新提示词置顶 |
| AI 对话 / Codex | 运行中显示已用上下文 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/interactive/codexTokenUsage.ts`、`src/interactive/codexRunner.ts`、`src/extensionHost/promptInteractiveRuntime.ts`、`src/webview/viewContentHtml.ts`、`src/webview/viewContentScript/taskListAndUi.ts`、`src/webview/viewContentScript/windowMessageDispatch.ts` | `npm run build`；`node --test dist/test/interactive/codexTokenUsage.test.js dist/test/interactive/codexRunnerLifecycle.test.js dist/test/extensionHost/promptInteractiveRuntime.test.js dist/test/webview/clipagescriptruntimecoverage.test.js dist/test/webview/cliPageStaticRenderCoverage.test.js` | Codex 任务执行时读取 app-server `thread/tokenUsage/updated`（以及 `turn/completed` 快照）的 `last`，按官方 `tokens_in_context_window() = totalTokens - reasoningOutputTokens` 计算当前窗口已用上下文，在底部“消息”按钮右侧以与“提示词”同高度的徽章显示“上下文：”前缀和 `k` 单位读数；Codex 开跑后先显示占位，有读数后保留到下次开跑前；仅主线程，Claude/OpenCode 不显示 |
| AI 对话 / 会话 | 会话 Tab 分页边界按钮 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/webview/viewContentScript/messageRendering.ts`、`src/webview/viewContentStyles/headerTabs.ts` | `npm run build`；`node --test dist/test/session/conversationTabPagination.test.js` | 超过 5 个 tab 时每页最多显示 5 个；第一页只显示“下一页”，中间页显示两个方向，最后一页只显示“上一页”；不可用方向直接隐藏，不渲染为 disabled 按钮 |
| AI 对话 / 会话 | OpenCode 任务 Tab 显示 opcode | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/webview/viewContentScript/messageRendering.ts` | `npm run build`；`node --test dist/test/session/conversationTabLock.test.js` | AI 对话发起 OpenCode 任务时，顶部任务 tab 的 CLI 标签显示为 `opcode`，同 CLI 多个 tab 依次为 `opcode2` 起；内部 `cli` 仍为 `opencode`，CLI 下拉和历史记录标识不变；Codex / Claude tab 仍显示原 CLI 名 |
| AI 对话 / 会话 | 历史会话 Loop/Graph 标记与统一恢复入口 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | 历史记录“历史会话”列表、Loop 任务与会话绑定、Graph run 与会话/消息绑定、输入区“打开群聊”按钮、底部“打开 Graph 图”按钮 | `npm run build`；`npm run test:page`；`node --test dist/test/graph/graphMainWebview.test.js dist/test/graph/graphStore.test.js` | 通过 Loop 任务持久化的 `cli + sessionId` 精确标记 Loop 会话；通过 Graph run store 的 `cli + sessionId` 或已保存消息中的 `graphRunId` / `openGraphRun` action 标记 Graph 会话。移除重复的“Loop 群聊”恢复 tab，加载会话后使用现有“打开群聊”按钮进入群聊；加载 Graph 会话后使用底部“打开 Graph 图”按钮进入运行图，后续普通用户回复不会清除当前 Graph run 入口 |
| AI 对话 / 配置中心 | 三组 CLI 可视化配置、参数提示与范例入口 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/exec-plans/active/2026-07-12-cli-config-visualization.md`、`.ch/docs/exec-plans/completed/2026-07/2026-07-31-codex-env-removal-keep-visual-editor.md`、`.ch/docs/references/cli-runtime-reference.md` | Claude / OpenCode / Codex 配置卡片可视化参数、配置文件名区域、配置页诊断说明 | `npm run build`；`node --test dist/test/config/codexConfigVisualEditor.test.js dist/test/config/claudeConfigVisualEditor.test.js dist/test/config/opencodeconfigvisualeditor.test.js`；raw value/排序定向 `dist/test`；`node --check media/config/assets/config-app-ui.js` | 三组可视化参数 label 右侧提供问号 tooltip，枚举提示列出当前候选，三组“查看范例”统一位于配置文件名右侧。Codex 保留 `config.toml` 可视化编辑和 TOML 源码模式，Provider 可视化不提供 `env_key` 输入；不展示、读取、写入、备份或导入导出 `.env`，也不删除用户已有文件，历史 `envContent` 在档案中忽略。Claude 将 `permissions.defaultMode` 升级为完整单选，并新增稳定布尔/行为字段；OpenCode 新增顶层 `share`/`autoupdate`/`logLevel`/`snapshot` 受控字段，`model` 改为与其他字段一致的可搜索单选下拉并占满可视化表单整行，候选来自当前配置；`small_model` 不再渲染配置控件但继续兼容保存，`npm` 保持可编辑组合框，reasoning effort 改为动态可输入 tags。点击配置档案默认进入源码模式（Claude/OpenCode 为 JSON，Codex 为 TOML），三组均保留原生 TOML/JSON 源码、未知值和复杂配置保护；`AugmentExtensionSidecar` 403 仍按外部扩展侧请求失败口径处理 |
| AI 对话 / OpenCode | OpenCode CLI、会话续接与配置驱动主/子模型 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | AI 对话 CLI 分组、OpenCode JSONL `sessionID` 提取、tab 会话接管、active config 模型解析、主模型/子模型下拉、按配置隔离的角色覆盖、运行时 config overlay、运行时 profile 日志、会话存档 | `npm run build`；`node --test dist/test/cli/opencodeCommandRunner.test.js dist/test/webview/opencodedualmodelwebview.test.js`；OpenCode parser/Webview/runner/runtime/store/config example tests | 首轮接管真实 `ses_*`，后续同 tab 通过 `--session` 续接；`local_*` 只用于插件消息暂存且不会传给 CLI，旧占位会话捕获新真实 ID后迁移消息和 tab 引用；候选只从 active config 的 `provider.<id>.models` 加载且无管理入口；聊天区 OpenCode 在 Loop/Graph 模式按两行展示主模型与子模型及各自思考力度，Vibe/coding 仅展示主模型与主模型思考力度，底层 `model` / `small_model` 仅作为 OpenCode CLI 配置字段适配；正常 option 只显示 `models.<id>.name`（缺失时回退 model id）；选择配置默认 ref 会清除临时覆盖，其他项发送 exact ref；Vibe 和普通并行任务使用 effective main，Loop 主任务、主持/复核和续跑使用 main，Loop 子任务使用 subtask，Graph planner 和最终 `summary` 节点使用 main，Graph 其他执行节点使用 subtask；Vibe 不读取或覆盖 `small_model`，Loop/Graph 子模型和两套思考力度才通过随机 `OPENCODE_CONFIG` overlay 注入；`opencode-runtime-profile` 及启动日志记录 main/subtask model 与两套 variant，Vibe 的 subtask 值为空；one-shot 只在启动后 60 秒完全无 activity 时超时，首个有效事件后不再以父 JSONL 静默判断卡死，避免分组/子代理任务误杀；覆盖按 config id 隔离并清理失效值，overlay 权限为目录 `0700`/文件 `0600`，exit/error/timeout/cancel 后清理且不改写用户配置 |
| AI 对话 / OpenCode | coding / Loop / Graph 模式入口 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/design-docs/vscode-cli-extension-runtime.md`、`.ch/docs/design-docs/graph-orchestration-mode.md` | 对话面板模式选择器、`src/sessionMessageActions.ts`、Loop 主任务/子任务/多轮复核/群聊编排、Graph `modelRouting`、active config effective main/subtask 运行链路 | `npm run build`；OpenCode Loop/Graph 模式入口与路由回归测试 | OpenCode 可在对话面板选择 coding、Loop 或 Graph；Loop 复用既有主任务、子任务、多轮和群聊链路，Graph 复用 realized DAG 编排链路，均通过非交互式 one-shot `opencode run` 执行。该能力不表示 `isInteractiveSupported(opencode)=true`，不启用 Codex/Claude interactive runner 或 common command；OpenCode 使用主模型/子模型角色执行 Loop 与 Graph，底层 `model` / `small_model` 仅作为 CLI 配置字段适配；Loop 多智能体执行模式下拉统一位于输入区底部操作图标左侧，与 Codex/Claude 布局一致；同一 tab 保留 Graph 图入口/元数据时，前台新发送仍跟随当前 UI 模式，切到 coding/Vibe 或 Loop 后复用会话上下文但不被自动路由回 Graph，后台队列和 Graph 续跑才允许按 Graph tab 元数据自动归入 Graph |
| AI 对话 / Loop/Graph | Codex/OpenCode 主/子模型路由与 CLI 模型职责 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md`、`.ch/docs/design-docs/vscode-cli-extension-runtime.md`、`.ch/docs/design-docs/graph-orchestration-mode.md` | Webview 模型区域、`sendPrompt.model`、`loopMainModel`、`loopSubtaskModel`、`loopMainThinkingMode`、`loopSubtaskThinkingMode`、OpenCode main/subtask 状态、Loop 普通轮次/辩论/续跑运行链路、Graph `modelRouting` / 节点 `modelRole` 记录、模型状态兼容读取 | `npm run build`；`node --test dist/test/webview/codexdualmodelwebview.test.js dist/test/session/sessionMessageActions.test.js dist/test/graph/graphExtensionRuntime.test.js dist/test/loop/loopPromptQueue.test.js dist/test/loop/continueModelChoice.test.js dist/test/loop/loopDebateCoordinator.test.js dist/test/webview/opencodedualmodelwebview.test.js dist/test/loop/opencodeloopmodewebview.test.js dist/test/extensionHost/opencoderolemodelruntime.test.js` | Claude 在 Coding/Loop 均不显示插件侧模型选择；Codex 普通 Coding 保持单模型，切到 Loop 或 Graph 时显示主模型/子模型两个选择器，并对 Codex 两个角色分别显示与保存思考力度；OpenCode 在 Loop/Graph 中显示主模型/子模型两个角色，Vibe/Coding 仅显示并使用主模型，底层 `model` / `small_model` 仅作为 OpenCode CLI 配置字段适配。Loop 主任务、主持/复核和续跑使用主模型及主任务思考力度，Loop 子任务使用子模型及子任务思考力度；Graph planner 和最终 `summary` 节点使用主模型及主任务思考力度，Graph 其他执行节点使用子模型及子任务思考力度；Vibe 不读取或覆盖子模型，缺少子模型不阻断 Vibe；Loop/Graph 缺少子模型时兼容回退到主模型或单模型并记录原因。主任务 Tab 显式继续仍以该 Tab 当前 CLI 分组为权威，读取该 CLI 当前激活配置和模型选择，并在跨 CLI 时迁移同一任务 ID 的 CLI/session Store 归属。Loop 与 Loop+ 群聊只有点击“继续”时才展示主/子模型选择；点击“我要说话”只提交补充消息，不展示该选择。继续时让用户选择恢复 `originProfile` 记录的原 CLI 分组、配置档案、主/子模型和思考力度，或改用当前 Loop 模式配置并写回 modelRouting 与 originProfile；没有 configId 或原模型记录时不能选择恢复原配置，配置档案缺失时明确失败且不新建任务。Graph 运行图继续时仍让用户选择保持已绑定主/子模型，或改用当前 Loop 模式配置并写回 modelRouting；未记录原模型时不能选择保持原模型；legacy lobster 与旧 Loop 主/子模型、OpenCode 旧 `primary` / `small` 字段仅用于兼容读取和迁移 |
| AI 对话 / Loop | 子任务思考力度全局上限 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md`、`.ch/docs/exec-plans/completed/2026-07/2026-07-15-loop-subtask-thinking-cap.md` | 工具设置“AI任务配置”Tab、`src/loopSubtaskThinking.ts`、`src/toolSettings.ts`、`src/sessionMessageActions.ts`、`src/panelStateBuilder.ts`、`src/extension.ts` | `npm run build`；`node --test dist/test/loop/loopSubtaskThinking.test.js dist/test/core/toolSettings.test.js dist/test/extensionHost/opencodethinkingintegration.test.js` | `loopSubtaskMaxThinkingMode` 保存到 `~/.sinitek_cli/settings.json`，默认 `xhigh`，可降低为 `low / medium / high`；每个 Loop 子任务以该上限和当前模型选择中的较低力度运行，`max` / `ultra` 最多为 `xhigh`；不改写主任务、普通任务或模型级用户选择 |
| AI 对话 / Loop | Loop 主任务 Tab 生命周期运行态与队列约束 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/design-docs/vscode-cli-extension-runtime.md` | `src/sessionTabs.ts`、`src/extension.ts`、`src/loopOrchestrationOwnership.ts`、`src/webview/viewContentScript/messageRendering.ts`、`src/webview/viewContentScript/runStreamAndQueue.ts` | `npm run build`；`node --test dist/test/session/conversationTabLock.test.js dist/test/loop/loopPromptQueue.test.js dist/test/session/sessionMessageActions.test.js dist/test/loop/loopDebate.test.js dist/test/loop/loopOrchestrationOwnership.test.js` | Loop 主任务 tab 使用 `☀️` 图标、子任务 tab 使用 `🌛` 图标，均不额外拼接 `Loop` 文本；切换 tab 时，只有带有效任务 ID 的 Loop 主任务自动切换到 Loop 模式，Loop 子任务和普通 tab 均自动切换到 Vibe；任务记录仍为 `running` 时，主 Tab 持续显示运行态并保持不可关闭，不依赖当前是否存在 AI/CLI 执行进程；原始流“慢”/“极慢”提示仅用于普通任务和 Loop 子任务，Loop 主任务不展示；`needs-review`、`error`、`stopped` 中断终态优先于异步释放中的旧编排所有权，立即结束视觉运行态并解除锁定，旧编排的轮次/重试派发门禁禁止把任务重新写回 `running`；同一任务并发退出/恢复的所有权按引用计数释放；该 tab 的新提示词直接入队且立即进入历史提示词列表，阶段性进程结束不提前出队，后续队列执行不重复记录历史；只有任务变为 `completed` 才自动继续队列，中断终态保留队列等待人工继续。群聊面板定时刷新只查找现有主 Tab，不为 `needs-review`、`error`、`stopped` 或 `completed` 任务重建已关闭主 Tab；只有用户明确继续才创建。其他正在运行的 Loop 任务不会唤醒已结束任务。 |
| AI 对话 / Loop | 可解析任务决策自动睡眠、持久化定时唤醒与群聊倒计时 | removed | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/design-docs/vscode-cli-extension-runtime.md`、`.ch/docs/design-docs/loop-debate-multi-agent-mode.md` | 旧 `src/loopAutoWake.ts` 已删除；兼容读取位于 `src/loopTaskStore.ts`；群聊 UI 位于 `src/webview/loopDebatePanel.ts` | `npm run build`；`node --test dist/test/loop/loopTaskStoreCoreCoverage.test.js dist/test/loop/loopDebate.test.js dist/test/loop/loopPromptBuilders.test.js dist/test/loop/loopDebatePanel.test.js dist/test/extensionHost/extensionDeactivateStopAll.test.js` | Loop 决策协议不再接受 `status=sleep`、`wakeAfterSeconds` 或 `sleepReason`；扩展不再创建 Loop 自动唤醒定时器，不再展示自动睡眠 assistant 气泡、群聊倒计时或计划唤醒时间。旧持久化 `sleeping` 任务读取时降级为 `needs-review`，并丢弃 `autoSleepStartedAt` / `autoWakeAt` / `autoSleepReason`，由用户通过现有“继续执行”手动恢复。 |
| AI 对话 / Loop | Loop 群聊用户任务与发言气泡 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | `src/loopDebate.ts`、`src/extensionHost/loopPlusSubtaskChat.ts`、`src/extensionHost/loopPlusOrchestration.ts`、`src/extension.ts`、`src/panelDiagnostics.ts`、`src/webview/loopDebatePanel.ts`、`src/webview/loopDebatePanelRenderer.ts`、`src/webview/loopDebatePanelStyles.ts` | `npm run build`；`node --test dist/test/loop/loopDebate.test.js dist/test/loop/loopDebatePanel.test.js dist/test/loop/loopPlusGroupChatPanel.test.js dist/test/extensionHost/loopPlusSubtaskChat.test.js dist/test/extensionHost/loopPlusOrchestration.test.js` | 创建或恢复任务时，任务记录中的原始 `rootPrompt` 会作为最顶部、标记为“任务发起”的右侧“我”气泡展示，不重复写入群聊 transcript；未完成且可补充需求的任务显示“我要说话”，提交内容写入任务记录和主通信文件，刷新后按提交顺序继续显示右侧“我”的对话气泡，并隐藏内部时间和轮次元数据。页面标题统一为“Loop 群聊”或“Loop+ 群聊”；成员列表不展示会话，改为最后启动时间，尚未启动显示未启动。Loop+ 群聊已确认的验收只显示验收成功或验收失败，不显示“已验收尝试”或内部 attempt。快照里仍有运行中子任务时，时间线末尾按经典 Loop 为每个运行中子任务显示“思考中”气泡和打字动画；主任务正在验收时再显示主任务“思考中”气泡。等待、排队未开始、暂停和停止都不会把主任务显示成正在生成；暂停或停止后尚未结束的运行中子任务仍保留执行动画。Loop+ 子任务启动时向群聊追加“子任务加入”，并在主任务聊天页留下与经典 Loop 相同的沟通文件消息；子任务结束时向群聊追加“子任务发言”，成功回复之外仍附带沟通文件路径，主任务聊天页同步显示该结束消息。 |
| AI 对话 / Loop / Loop+ | 群聊沟通文件 Markdown 预览 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/design-docs/loop-debate-multi-agent-mode.md` | `src/webview/loopDebatePanel.ts`、`src/loopCommunicationFilePreview.ts`、`src/panelDiagnostics.ts` | `npm run build`；`node --test dist/test/loop/loopCommunicationFilePreview.test.js dist/test/loop/loopDebatePanel.test.js dist/test/loop/loopDebateCoordinator.test.js` | Loop 与 Loop+ 群聊正文中的“沟通文件”绝对路径显示为链接。点击后弹窗读取并按普通 Markdown 展示该文件；只允许当前任务沟通目录内的 .md。目录外、缺失、空文件、过大或不可读时在弹窗内提示，不打开任意本地文件 |
| AI 对话 / OpenCode | 精确主/子模型动态推理 variant | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/cli/openCodeModelCapabilities.ts`、`src/modelSelectionStore.ts`、`src/panelStateBuilder.ts`、`src/extension.ts`、`src/cli/commandRunner.ts`、Webview thinking selector | `npm run build`；相关 OpenCode resolver/config/Webview/runner/action/integration tests | Loop/Graph 的主模型与子模型思考力度分别来自对应精确模型 CLI metadata 或当前配置显式声明；Vibe/Coding 仅应用主模型思考力度。主模型可用 `--variant`，子模型因 CLI 无 `--small-variant`，仅在 Loop/Graph 由 runtime config overlay 的 `small_model` 兼容字段写入对应模型 `options.reasoningEffort`；未知模型 Default-only，不按 npm/provider/model 名猜测，`--thinking` 仅控制 thinking blocks |
| AI 对话 / 配置 | 配置切换提交后生效与发送门禁 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/exec-plans/completed/2026-08/2026-08-28-config-switch-consistency-audit.md` | `src/config/configApplyQueue.ts`、`src/extension.ts`、`src/extensionHost/modelSettings.ts`、`src/sessionMessageActions.ts`、`src/sessionMessageHandlers.ts`、`src/webview/viewContentScript/modelAndPanelState.ts`、`src/webview/viewContentScript/runStreamAndQueue.ts`、`src/webview/viewContentScript/taskListAndUi.ts`、`src/webview/viewContentScript/windowMessageDispatch.ts`、`src/webview/viewContentI18n.ts` | `npm run build`；`node --test dist/test/config/configApplyQueue.test.js dist/test/session/sessionMessageActions.test.js dist/test/session/sessionMessageHandlersCoreCoverage.test.js dist/test/webview/clipagescriptruntimecoverage.test.js` | 配置下拉切换先进入待生效态，宿主完成异步 `applyConfig` 并回写 `activeConfigIdByCli` 后才算真正切换；最新选择覆盖更早的待提交请求，发送与队列出队在 `pendingConfigApply` 期间会等待，失败时回滚到当前 active config 并提示错误，避免 UI 显示与实际执行配置脱节 |
| AI 对话 / Gemini | Gemini CLI 支持下线 | removed | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | 旧 AI 对话 Gemini 分组、旧配置中心 Gemini 卡片、旧 `geminiStreamJson` / catalog 路径 | 文档校验：`rg -n "Gemini\|gemini" <授权文档>`；源码验证由对应实现子任务负责 | Gemini 已从当前 AI 对话和配置中心支持范围移除；官方 Skills catalog 条目、内置 ZIP 和 legacy 同步入口也已删除，历史只通过 Git 与归档文档审计 |
| AI 对话 / 上下文 | 全局执行后自动压缩上下文 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md`、`.ch/docs/exec-plans/completed/2026-07/2026-07-13-global-auto-compact-setting.md` | 工具设置“AI任务配置”Tab、`src/toolSettings.ts`、`src/workspaceSettingsStore.ts`、`src/sessionMessageActions.ts`、`src/contextCompactionRunner.ts`、`src/extension.ts`、Codex/Claude/OpenCode interactive runner 或 OpenCode 非交互 slash command fallback | `npm run build`；`node --test dist/test/core/toolSettings.test.js dist/test/core/workspaceSettingsStore.test.js dist/test/session/sessionMessageActions.test.js dist/test/webview/multiAgentSettingWebview.test.js dist/test/core/contextCompactionRunner.test.js dist/test/extensionHost/opencodethinkingintegration.test.js` | `autoCompactContextAfterRun` 保存在 `~/.sinitek_cli/settings.json`，默认开启；旧工作区 after-run/before-run 字段仅作为迁移输入，全局值优先，成功迁移或用户更新后移除当前工作区旧字段。仅成功结束且执行超过 5 分钟的已有会话触发；自动压缩为静默后台任务，单次最多等待 3 分钟，超时停止当前压缩并按未压缩处理，不追加普通任务完成耗时气泡、不覆盖刚完成任务的真实执行时间；手动压缩仍显示压缩运行状态；OpenCode 官方 TUI slash commands 支持 `/compact`（alias `/summarize`）用于 compact current session，配置 `compaction.auto` 默认开启，插件侧可复用当前实现链路或在非交互受限时 fallback 发送官方 slash command |
| 配置 / 配置中心 | CLI 配置档案管理与卡片级保存 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/exec-plans/active/2026-07-12-cli-config-visualization.md`、`.ch/docs/exec-plans/completed/2026-07/2026-07-31-codex-env-removal-keep-visual-editor.md`、`.ch/docs/exec-plans/completed/2026-07/2026-07-31-vscode-config-fullscreen-panel.md`、`.ch/docs/exec-plans/active/2026-07-31-vscode-config-zen-mode-popup.md` | 前台聚焦的 VS Code `WebviewPanel`、Zen Mode 宿主、`media/config/assets/config-app-ui.js`、`src/config/configService.ts`、`src/webview/configPanel.ts`、`src/webview/configView.ts` | `npm run build`；`node --check media/config/assets/config-app-ui.js`；`node --test dist/test/config/configService.test.js dist/test/config/cliPageConfigCoverage.test.js dist/test/config/codexConfigVisualEditor.test.js dist/test/config/claudeConfigVisualEditor.test.js dist/test/config/opencodeconfigvisualeditor.test.js` | 从 AI 对话面板点击“配置”立即打开并前台聚焦 VS Code 编辑器主区域的配置面板，并尝试进入 Zen Mode 隐藏工作台外围以接近全屏弹窗；重复点击复用同一面板且不重复切换，关闭后恢复由本插件进入的 Zen Mode，命令不可用时回退为普通编辑器面板。VS Code 扩展公开 API 不提供任意 Webview 的原生模态弹窗，因此不启动独立浏览器或本地 HTTP 页面。面板继续支持配置档案列表、排序、激活、删除、初始化、备份、导出、Skills 和 MCP 管理，点击或新增选择配置档案时默认打开源码模式。配置页布局和交互尽量保持携宁 CLI 配置页一致，同时铺满 WebviewPanel 剩余视口，大型配置浮层使用近满屏宽高以减少左右和底部空白；配置页三组视觉编辑器和复制入口的单值下拉统一使用与主模型 `model` 一致的可搜索、可清除 Ant Design `Select`；颜色、字体、边框、焦点、状态、弹层和背景全部映射到 VS Code `--vscode-*` 语义变量，并在 VS Code 主题切换时重新解析 Ant Design token。Codex 卡片管理 `~/.codex/config.toml` 和既有 `auth.json` 入口，保持 TOML 可视化/源码模式，Provider 可视化不提供 `env_key` 输入，受控单选、布尔和多行文本仅定向写入已管理字段，复杂 TOML 与 legacy/未知值保留；不展示、读取、写入、备份或导入导出 `.env`，也不删除用户已有文件。Claude 卡片管理用户级 `~/.claude/settings.json`，保持 JSON 可视化/源码模式；稳定 boolean、行为单选、完整 `permissions.defaultMode`、模型/网关自由文本和可编辑列表并存，额外 `env`、hooks、复杂权限和未知字段不丢失。OpenCode 模型/Provider 卡片只维护 `~/.opencode/config.json`，全局 MCP 市场单独维护 `${XDG_CONFIG_HOME:-~/.config}/opencode/opencode.json` 顶层 `mcp`，不提供 `.env` 第二保存入口；其顶层稳定字段使用继承语义控件，`model` 使用当前配置候选的可搜索单选下拉，`small_model` 不显示编辑入口但兼容保留，`npm` 使用同一 Select 的可搜索单值模式，提供 OpenCode 官方 `BUNDLED_PROVIDERS` 的 24 个 npm loader 建议并允许任意自定义 npm 包名，reasoning effort 使用动态可输入多值 tags，未知顶层/provider/model/options/复杂 variants 与无效 JSON 保护保持。思考力度显示 raw value；Codex 固定新建值为 `minimal / low / medium / high / xhigh / max / ultra`，`max` 紧跟 `xhigh`；Claude 固定新建值以 `ultra` 结束，存量 `max` 仅兼容显示在 `ultra` 前；OpenCode 精确 provider/model 动态 variants 保持 payload 原值和顺序 |
| 配置 / MCP | OpenCode 官方全局配置文件 MCP 管理 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md` | `src/config/openCodeMcpConfig.ts`、`src/config/mcpService.ts`、MCP 市场、`media/mcp_marketplace.json`、`scripts/validate_mcp_marketplace.js` | `npm run validate:mcp-marketplace`；`npm run build`；`node --test dist/test/config/openCodeMcpConfig.test.js dist/test/config/configMcpService.test.js dist/test/config/opencodeMcpHealth.test.js dist/test/config/mcpInstalledStatus.test.js dist/test/config/mcpMarketplaceCatalog.test.js` | 安装和卸载直接管理 `${XDG_CONFIG_HOME:-~/.config}/opencode/opencode.json` 顶层 `mcp`；local 使用命令数组与 `environment`，remote 使用 URL 与 `headers`；安装保留其他顶层字段和已有 MCP，卸载只删除目标键且幂等；JSON/JSONC 解析失败不覆盖原文件，成功修改原子写回；MCP 市场打开时仅从 Claude / Codex / OpenCode 各自配置文件快速读取安装状态，健康检测由按钮单独触发，`opencode mcp list --pure` 仅用于 OpenCode 健康检测；内置市场已压缩刷新为 GitHub、Microsoft Learn、Playwright、Docker Gateway、Cloudflare、Stripe、Sentry、MongoDB、Grafana、Elastic、Slack、Notion、Linear、Brave、Atlassian 等官方/权威候选，校验脚本阻止旧 `modelcontextprotocol/servers/tree/main/src/` 路径和旧 `@modelcontextprotocol/server-*` 包名回流 |
| 配置 / 配置中心 | 窄屏配置目录初始展开 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` | 对话面板配置按钮、`media/config/assets/config-app-ui.js`、配置中心 `920px` 响应式布局 | `npm run build`；`node --check media/config/assets/config-app-ui.js`；`node --test dist/test/config/configappcompactcontrols.test.js` | 从配置按钮首次打开配置中心且当前视口小于等于 `920px` 时，左侧配置目录默认展开；用户仍可通过关闭按钮、遮罩或 `Esc` 收起，宽屏初始状态不变 |
| AI 对话 / Loop | Loop 命名与本地存储迁移 | active | 终端用户 | `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md`、`.ch/docs/references/cli-runtime-reference.md`、`.ch/docs/design-docs/vscode-cli-extension-runtime.md` | `src/loopLegacyMigration.ts`、`src/loopTaskStore.ts`、设置/模型/任务运行/会话消息读取边界、`sinitek-cli-tools.openLoopGroupChat` | `npm run build`；`node --test dist/test/loop/loopLegacyMigration.test.js dist/test/loop/loopTaskStore.test.js dist/test/core/toolSettings.test.js dist/test/core/workspaceSettingsStore.test.js dist/test/extensionHost/opencoderolemodelruntime.test.js dist/test/core/codexReasoningContent.test.js` | 新任务和通信 artifact 只写入 `loop-tasks/.../loop-tasks.json` 与 `loop-communications/<taskId>/`；升级后首次任务枚举会迁移旧 Lobster 目录与记录，持久化旧前缀键按新键优先归一化；群聊公开命令改为 `openLoopGroupChat`，旧命令仅保留隐藏兼容别名 |
