# 龙虾并发子任务

- 日期：2026-05-05
- 状态：completed
- 负责人：Codex

## 背景

当前龙虾模式主任务每轮只能返回一个 `subtask`，扩展启动该子任务并等待完成后立即唤醒主任务复核。用户要求改为由主任务判断子任务是否互不冲突；不冲突时可以同一轮并发执行多个子任务，并且必须等这一批子任务全部完成后才唤醒主任务。

## 目标

支持龙虾主任务在 `status=continue` 时返回一个并发子任务批次。扩展为批次内每个子任务创建独立会话并并发执行；仅当批次内所有子任务都正常完成后，才追加主任务唤醒消息并进入下一轮主任务复核。

## 范围

- 扩展龙虾主任务 JSON 协议，兼容旧的单 `subtask` 字段，新增 `subtasks` 批次字段。
- 更新任务记录与轮次记录，支持同一轮多个子任务的状态与沟通文件。
- 更新龙虾主/子任务提示词，要求主任务负责冲突判断，只有无共享写入/验证冲突时才批量派发。
- 同步功能清单与 CLI 运行时参考。

## 非目标

- 不在扩展侧做源码级冲突分析或自动拆分依赖图。
- 不改变 CLI 技术栈、模型配置 UI 或底层交互 Runner。
- 不实现跨 VS Code 重启后的未完成批次自动恢复。

## 验收标准

- [x] 旧协议 `{ "status": "continue", "subtask": ... }` 仍能启动一个子任务。
- [x] 新协议 `{ "status": "continue", "subtasks": [...] }` 能启动多个独立子任务会话。
- [x] 批次内任一子任务未结束前不会唤醒主任务。
- [x] 批次全部正常完成后才追加主任务复核提示，并进入下一主任务轮。
- [x] 子任务出错/停止时任务进入人工复核或中断状态，不提前唤醒主任务。
- [x] `npm run build` 通过。

## 影响面

- 代码目录：`src/extension.ts`
- 文档目录：`.ch/docs/product-specs/`, `.ch/docs/references/`
- 配置与脚本：无新增依赖；保留现有构建命令

## 风险与缓解

- 风险：多个交互式子任务同时运行时可能受已有多 tab 并发隔离影响。
- 缓解：继续使用每个子任务独立 conversation tab 和现有 `interactiveRunsByTabId` 隔离；批次调度只聚合 Promise 结果，不复用同一会话。

- 风险：模型误把冲突任务放进同一批次。
- 缓解：协议提示明确主任务负责冲突判断；扩展只信任主任务声明，不做猜测式冲突检测。

## 验证计划

- 最小相关验证：`npm run build`
- 扩展验证：人工在龙虾模式让主任务返回 `subtasks` 批次，观察多个子任务 tab 并发启动，并确认全部结束后才唤醒主任务。

## 测试与清单同步

- 单元测试：当前龙虾调度逻辑集中在 VS Code 扩展闭包内，缺少可直接导入的单元测试入口；本轮以 TypeScript 构建和文档同步为最低验证，后续可抽出纯协议解析模块补测。
- 功能清单：已更新 `.ch/docs/product-specs/FEATURE_INVENTORY.md`
- 相关文档同步：已更新 `.ch/docs/product-specs/sinitek-cli-plugin-capabilities.md` 与 `.ch/docs/references/cli-runtime-reference.md`

## 任务列表

- [x] 定位龙虾模式串行子任务实现
- [x] 扩展协议与数据结构
- [x] 实现并发批次调度与全部完成后唤醒
- [x] 同步事实来源文档
- [x] 运行构建验证

## 决策记录

- 2026-05-05：扩展侧不做文件冲突分析，由主任务在 JSON 决策中只把互不冲突的子任务放入同一 `subtasks` 批次。
- 2026-05-05：保留旧 `subtask` 字段兼容；新实现内部统一成子任务数组处理。
- 2026-05-05：同一批次设置最多 6 个子任务，防止模型一次性派发过多并发会话压垮 UI 和本地 CLI。

## 当前结论

已完成实现与文档同步。`src/extension.ts` 现在兼容旧 `subtask`，并支持新 `subtasks` 批次；多子任务批次并发启动独立 tab / 会话，只有全部返回 `end` 后才追加主任务唤醒消息，出错或停止则进入人工复核/中断路径。验证：`npm run build` 通过。
